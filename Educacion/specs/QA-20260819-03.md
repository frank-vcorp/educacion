# AUDITORÍA QA — IMPL-20260819-03 (cierre P3-N1 + P3-N2 + P3-N3 L1; P3-N4 aceptado, post QA-20260819-02)

- **Auditoría:** QA-20260819-03
- **Fecha:** 2026-08-19 (inicio 19:24 CST, cierre 19:33 CST)
- **Auditor:** GEMINI (independiente; instruido por Frank sobre el reporte `READY_FOR_VERIFYING` de SOFIA)
- **SPEC/ADR:**
  - `specs/ADR-20260819-01.md` (ARCH-20260819-01, ACEPTADO) — Addendum "Cierre P3 tras QA-20260819-02": N1/N2 refinements, N3 restauración de contrato del handoff P2-FIXES, N4 aceptación documentada.
  - `specs/SPEC-HANDOFF-20260819-SOFIA-P3-FIXES.md` — criterios AC-P3-1..AC-P3-6 + AC-G1..AC-G4.
  - `specs/SPEC-HANDOFF-20260819-SOFIA-P2-FIXES.md` Fix 2 punto 4 — contrato verbatim del mensaje de fallo de compensación (protegido).
  - `specs/QA-20260819-02.md` — auditoría origen (PASS_WITH_WARNINGS; P3-N1..N4).
  - `specs/SPEC_TEC_03_API_Contract.md` §6.6/§6.30 (sin cambio verificado); `fuentes/E22_CIERRE_DISCOVERY.md` §D-FIN-5, §D-FIN-17 (preservación verificada).
- **Incremento:** IMPL-20260819-03 en working tree `/home/frank/repos/educacion/Educacion`. **Sin commit/PR** (app untracked; HEAD `976f757` — igual que QA-01/02; restricción P3-5 vigente). Delimitado por `find -newermt` (ventana 19:15–19:23 de 2026-08-19), corroborado archivo por archivo contra el inventario del reporte IMPL:
  - `services/planeaciones/planeacion-actions.ts` (19:15) — N1 + N3-i + N3-ii.
  - `lib/pdf/generate.ts` (19:16) — N2.
  - `tests/unit/services/planeaciones/duplicate-compensation.test.ts` (19:17) — +3 tests + flags del mock.
  - `tests/unit/lib/pdf-generate.test.ts` (19:18) — +1 test.
  - `specs/IMPL-20260819-03_report.md` (19:23, NEW).
  - `specs/ADR-20260819-01.md` (19:11) y `specs/SPEC-HANDOFF-20260819-SOFIA-P3-FIXES.md` (19:12) son artefactos de INTEGRA previos al código, no parte del incremento de SOFIA.
  - Cero archivos fuera de inventario en la ventana (find global excluyendo node_modules/.next/.git/.cache/.vercel).
- **Veredicto:** **PASS**
- **Alcance:** estándar (fixes L1 acotados sobre 2 módulos + tests; sin contrato público nuevo, sin migraciones, sin dependencias; el único cambio de cadena de error ya estaba mandado por el handoff P2-FIXES).

---

## A. Delimitación y fuentes

| Aspecto | Evidencia |
|---|---|
| Base/cabeza | Sin rango git (app untracked, P3-5 vigente). Cabeza = working tree al 2026-08-19 19:23. |
| Delimitación | `find -newermt '2026-08-19 19:10'` global: 7 archivos (2 specs de INTEGRA + 4 código/tests de SOFIA + 1 reporte). Coincide 1:1 con el inventario del reporte IMPL (sección "Archivos modificados"). |
| Protegidos | `discovery/`, `fuentes/` — 0 archivos con mtime ≥ 19:00. `SPEC_MVP_01_Modulo_Docente.md` (mtime 2026-08-18), `package.json`/`pnpm-lock.yaml` (2026-08-17), `tsconfig.json`/`playwright.config.ts`/`next.config.mjs` (2026-08-17), `vitest.config.ts` (17:52, ventana IMPL-01 ya documentada), `supabase/migrations/*.sql` (2026-08-17/18) — todos fuera de la ventana IMPL-03. |
| Nota git | `git status` muestra cambios tracked preexistentes (`M SPEC_MVP_01_Modulo_Docente.md` mtime 08-18, `M fuentes/README.md` mtime 08-15, renames `catalogar/ → scripts/catalogar/`) — todos anteriores a este incremento; ya presentes en QA-01/02. |
| Limitaciones | (1) Sin Supabase real: RLS cross-tenant SKIPPED (preexistente). (2) E2E Playwright no ejecutado (requiere servidor + Supabase). (3) Compensación verificada contra mock fiel al contrato de filtros supabase-js, no contra Postgres real. Mismo régimen que QA-02, autorizado por el handoff. |

---

## B. Trazabilidad (criterio → implementación → evidencia independiente → resultado)

### Unidad A — P3-N2: lifecycle de `newPage()`

| AC | Implementación | Evidencia independiente GEMINI | Resultado |
|---|---|---|---|
| **AC-P3-2** fallo de `newPage()` → `browser.close()` 1 vez + error propaga | `lib/pdf/generate.ts:333-360` — `let page` fuera del try (340), `newPage()` dentro (342), guard `page?.close()` (357), `browser.close()` siempre (358) | Test AC-P3-2 re-ejecutado en verbose (PASS): `rejects.toBe(newPageError)` — **identidad** del error, no sólo mensaje; spy `closeBrowser` llamado exactamente 1 vez. Lectura de código: el `.catch(() => undefined)` sólo envuelve los cierres; el error del try propaga tras el finally (el finally no lanza). | **PASS** |
| **AC-P3-2b** no-regresión AC-7 (3 tests previos) | sin cambio en su lógica | Los 3 tests re-ejecutados en verbose (PASS): newPage OK → close 1 vez; error de `browser.close()` tolerado; error de `page.close()` tolerado. Caso borde del handoff ("newPage lanza Y browser.close también lanza") cubierto por construcción: el `.catch` del cierre nunca propaga; el test AC-P3-2 usa close exitoso y el AC-7 previo cubre close fallando con newPage OK — la combinación queda cubierta por ambos mecanismos. | **PASS** |

### Unidad B — P3-N1 + P3-N3: compensación errSes + mensaje/errores intermedios

| AC | Implementación | Evidencia independiente GEMINI | Resultado |
|---|---|---|---|
| **AC-P3-1 (N1)** SELECT sesiones origen falla tras insert de planeacion → compensación ejecuta, store termina con 0 planeaciones | `planeacion-actions.ts:487-507` — `if (errSes)` invoca `compensarClonado` antes del return | Verificación estructural: `sesionesCreadas` se inicializa vacío (línea 477) y el primer `push` ocurre en línea 552 (loop de copia), por lo que en el path `errSes` la compensación sólo puede borrar la planeación recién creada — coincide con el ADR addendum. Test AC-P3-1 re-ejecutado (PASS): `r.ok === false`, `r.error` matchea el error **original** del SELECT (compensación exitosa → booleano true), `store.inserted.planeacion/sesion/bloque` = 0. `console.warn` "borradas filas del intento … (0 sesiones)" observado en stderr. | **PASS** |
| **AC-P3-3 (N3-i)** `compensarClonado` retorna `false` → call sites devuelven mensaje verbatim | 4 call sites capturan `const compensada`: `errSes` (492-505), `errInsSes` (535-545), `errBloque` (569-579), `errInsBloque` (603-613) | Grep del mensaje verbatim: **4 ocurrencias** en producción (líneas 503, 543, 577, 611), cadena idéntica byte a byte al handoff P2-FIXES Fix 2 punto 4 (`Falló la compensación del clonado; revisar filas huérfanas para planeacion_id=<id>` con `${nuevaPlaneacionId}`). Test AC-P3-3 re-ejecutado (PASS): `r.error === '…planeacion_id=00000000-0000-4000-8000-000000000001'` (UUID determinista del mock). | **PASS** |
| **AC-P3-4 (N3-ii)** sólo delete de `bloque` falla → booleano `false` (error intermedio inspeccionado) | `compensarClonado:672-727` — desestructura `{error}` de los 3 deletes (bloque 680-690, sesion 693-703, planeacion 705-715); cada fallo → `console.warn` con `nuevaPlaneacionId` + tabla → `return false`; `true` sólo si todos los aplicables OK; `catch` externo intacto (720-726) | Test AC-P3-4 re-ejecutado (PASS): con `deleteBloqueFail=true` y sesionesCreadas no vacío, `r.error` es el mensaje verbatim — demuestra que el error del delete de **bloque** (intermedio, antes no inspeccionado) vuelve `false` el booleano. `console.warn` "error borrando bloque(s) para planeacion_id=…" observado en stderr (antes del fix sólo existía el warn de planeacion). | **PASS** |
| **AC-P3-4b** no-regresión AC-4 (casos A/B/C/D devuelven error original) | sin cambio en sus mocks/aserciones | Los 4 casos re-ejecutados en verbose (PASS): compensación exitosa en sus mocks → booleano `true` → error original devuelto (`mock insert failed: …`). | **PASS** |
| **AC-P3-5** no-regresión AC-5 (sólo IDs del intento) | filtros intactos: `in('sesion_id', sesionesCreadas)` (683), `eq('planeacion_id', nuevaPlaneacionId)` (696), `eq('id', nuevaPlaneacionId)` (708) | Test AC-5 re-ejecutado (PASS): fila ajena sembrada sobrevive. Revisión de código: ningún delete sin filtro. | **PASS** |
| **AC-P3-6** no-regresión AC-6 (path de éxito) | pasos 4-7 sin cambio | `duplicate.test.ts` (8 tests) + `duplicate.integration.test.ts` (4 tests) PASS en la suite completa. Semántica D-FIN-17 intacta: `evaluacionesCopiadas=0` + warning `copiarEvaluaciones=true no soportado` observado en stderr. | **PASS** |

### Validaciones globales

| AC | Evidencia independiente GEMINI | Resultado |
|---|---|---|
| **AC-G1** typecheck | `pnpm typecheck` ejecutado por GEMINI → exit 0, sin errores. | **PASS** |
| **AC-G2** lint | `pnpm lint` ejecutado por GEMINI → exit 0; 1 warning en `lib/supabase/server.ts:11` (preexistente, documentado desde QA-01; archivo no tocado por este IMPL). | **PASS** |
| **AC-G3** tests sin regresión | `PDF_GENERATOR=playwright pnpm test` ejecutado por GEMINI → **12 files passed \| 1 skipped; 90 passed \| 2 skipped** (baseline QA-02: 86+2 → +4 tests nuevos: AC-P3-1/2/3/4; cero fallas). Coincide con el reporte IMPL. | **PASS** |
| **AC-G4** build | `pnpm build` ejecutado por GEMINI → exit 0; `route.js` de `/api/planeaciones/[id]/generar-pdf` presente en `.next/server/app/`; `/v/[entrega_id]` en el listado de rutas. | **PASS** |

### Verificación de los 4 puntos específicos solicitados por Frank

1. **P3-N1 compensación en errSes:** verificado (§B Unidad B, AC-P3-1). El path de lectura que antes dejaba planeación huérfana ahora compensa; el objetivo "cero filas huérfanas" de la decisión 2 del ADR queda cubierto en los 5 paths de fallo post-insert (4 de escritura + 1 de lectura).
2. **P3-N2 cierre de browser al fallar newPage:** verificado (§B Unidad A). `newPage()` dentro del try; fallo → `browser.close()` 1 vez + error propagado por identidad. Evidencia adicional en entorno real: tras la suite completa (smoke con chromium real incluido), `ps aux` no muestra procesos chromium del renderer (los matches son servidores MCP de Kilo, previos a la corrida). Cero huérfanos.
3. **P3-N3 detección/propagación contractual de fallos de compensación:** verificado (§B Unidad B, AC-P3-3/AC-P3-4). Las dos partes acopladas cerradas juntas: booleano exacto (3 deletes inspeccionados) + 4 call sites que lo capturan y devuelven el mensaje verbatim del handoff P2-FIXES. El único consumidor UI (`duplicar-planeacion-dialog.tsx:92-94`) muestra `res.error` genéricamente → sin acoplamiento al texto previo, cambio seguro.
4. **P3-N4 aceptado sin regresión:** verificado. Los tests AC-1 (source-text `Function.toString()`) y AC-2 (cadena exacta "19 de agosto de 2026") permanecen **sin modificación** (lectura directa `pdf-generate.test.ts:210-250`), consistente con la aceptación documentada del ADR addendum (owner INTEGRA, 2026-08-19; condición de re-evaluación: CI externo/heterogéneo). Ambos siguen PASS en el entorno actual (CST + ICU es-MX + vitest sin minificar). Sin código nuevo para N4.

---

## C. Validaciones independientes (ejecutadas por GEMINI)

| # | Comando | Exit code | Resultado |
|---|---|---|---|
| 1 | `pnpm typecheck` | 0 | PASS, sin errores. |
| 2 | `pnpm lint` | 0 | PASS; 1 warning preexistente (`lib/supabase/server.ts:11`), no introducido. |
| 3 | `PDF_GENERATOR=playwright pnpm test` | 0 | **12 files passed \| 1 skipped; 90 tests passed \| 2 skipped.** Coincide con el reporte IMPL. |
| 4 | Re-ejecución verbose `duplicate-compensation.test.ts` + `pdf-generate.test.ts` | 0 | 33/33 PASS; los 4 tests nuevos (AC-P3-1/2/3/4) + AC-4 A/B/C/D + AC-5 + AC-7 (3) + AC-1/AC-2 (5) confirmados por nombre. |
| 5 | Smoke chromium real (dentro de #3) | 0 | Render real: **36 888 bytes en ~1 819 ms**, `sha256=ed735378bb0e5353…` |
| 6 | `file /tmp/kilo/smoke-planeacion.pdf` | 0 | `PDF document, version 1.4, 1 page(s)` (D-FIN-5 preservado). |
| 7 | `sha256sum /tmp/kilo/smoke-planeacion.pdf` | 0 | `ed735378bb0e5353103c95726ea6d2e688575f5715ccef53ec428604423ce33f` — coincide con el hash emitido por el test: el `X-Pdf-Sha256` es el SHA-256 real del binario. |
| 8 | `od -c` cabecera | 0 | Magic `%PDF-1.4` verificado byte a byte. |
| 9 | `strings` sobre el PDF | 0 | `/CreationDate (D:20260820012719+00'00')` y `/ModDate` presentes → causa (b) residual sigue presente, como predice el ADR decisión 1 (hash GEMINI `ed735378…` ≠ hashes de corridas previas). |
| 10 | `ps aux` post-suite | 0 | Cero procesos chromium del renderer tras el smoke real (N2 + P2-3 verificados en entorno real). |
| 11 | `pnpm build` | 0 | PASS; endpoint `generar-pdf` (`route.js`) y `/v/[entrega_id]` presentes. |
| 12 | `rg` mensaje verbatim | 0 | 4 ocurrencias en producción (503/543/577/611) + handoff P2-FIXES: cadena idéntica; 2 aserciones verbatim en tests (630/655) + 1 `toContain` (658). |
| 13 | `rg` no-export + independencia | 0 | `compensarClonado` es función interna (sin `export`); 0 imports cruzados entre `lib/pdf/generate.ts` y `planeacion-actions.ts` (confirma la independencia Unidad A/B del handoff). |
| 14 | `find -newermt` delimitación + protegidos | 0 | Ventana IMPL-03 = 5 archivos (19:15–19:23); protegidos intactos (§A). |
| 15 | `rg "new Date\(\)" lib/pdf/generate.ts` | 0 | Sólo comentarios; única construcción `Date` del módulo sigue siendo `new Date(iso)` en `formatFechaEsMx` (parser determinista). AC-1 de QA-02 sin regresión. |
| 16 | `rg updated_at` callers | 0 | Ambos callers (`route.ts:44`, `entrega-actions.ts:61`) siguen seleccionando `updated_at` (AC-3 de QA-02 sin regresión). |

**No ejecutadas (documentado):** E2E Playwright (T-E2E-05), RLS cross-tenant contra Postgres real, roundtrip HTTP con sesión real, compensación contra Postgres real (verificada contra mock fiel). Razón: sandbox sin Supabase/servidor; el handoff autoriza declarar "NO EJECUTADA".

---

## D. Hallazgos priorizados

### Nuevos (introducidos/detectados en este incremento)

**Ninguno.** P0: 0 · P1: 0 · P2: 0 · P3: 0.

Notas informativas (no hallazgos, no requieren acción):

- **N-I-1 (informativa):** el corto-circuito de `compensarClonado` ante fallo de un delete intermedio (no intenta los deletes restantes) es exactamente lo especificado por el handoff P3-FIXES ("si cualquiera devuelve `error` … retorna `false`"); las filas restantes quedan cubiertas por el mensaje verbatim que orienta a revisar huérfanas con `planeacion_id`. El cierre total transaccional (RPC) sigue diferido a `ARCH-20260819-02`.
- **N-I-2 (informativa):** el test nuevo se nombra "AC-P3-2 (N2): …" mientras el handoff anticipaba el nombre "AC-7: si newPage() lanza…"; diferencia cosmética de nomenclatura, el comportamiento exigido está cubierto y el nombre real es más preciso (alineado al ID de AC).

### Cierre de hallazgos de QA-20260819-02

| ID QA-02 | Estado tras IMPL-03 | Evidencia |
|---|---|---|
| **P3-N1** (errSes sin compensación) | **CERRADO** | §B AC-P3-1: compensación invocada en el path errSes; store termina con 0 filas; test dedicado PASS. |
| **P3-N2** (`newPage()` fuera del try) | **CERRADO** | §B AC-P3-2: fallo de `newPage()` cierra el browser y propaga el error; test dedicado PASS; 0 chromium huérfanos post-smoke real. |
| **P3-N3** (mensaje + errores intermedios) | **CERRADO** | §B AC-P3-3/AC-P3-4: booleano exacto (3 deletes inspeccionados) + 4 call sites con mensaje verbatim; contrato del handoff P2-FIXES Fix 2 punto 4 restaurado. |
| **P3-N4** (fragilidad TZ/ICU/source-text) | **ACEPTADO — sin regresión** | Tests AC-1/AC-2 sin modificación y PASS en entorno actual; aceptación documentada vigente (ADR addendum; owner INTEGRA; re-evaluación al existir CI heterogéneo). |

### Residuales de QA-20260819-01 (estado sin cambio, aceptados/diferidos)

| ID QA-01 | Estado |
|---|---|
| **P2-1 causa (b)** (metadata Chromium embebida) | Residual aceptado por ADR decisión 1; re-confirmado en esta corrida (hash `ed735378…` difiere entre corridas; `/CreationDate` presente). Cierre total → `ARCH-20260819-02` (Storage). |
| **P2-2** (compensación no atómica a nivel DB) | Ventana teórica si el proceso muere a mitad de compensación — aceptada por ADR decisión 2; cierre total → RPC en `ARCH-20260819-02`. |
| **P3-1** (sin `getServerSession()` en action) | Abierto, fuera de alcance por instrucción de Frank; RLS es el boundary real. |
| **P3-2** (E2E pendiente) | Abierto — gate de staging/producción (requiere entorno con Supabase). |
| **P3-4** (`sesion_recurso` no clonada) | Abierto (nota futura; tabla no escrita por la app). |
| **P3-5** (incremento sin commit) | Abierto — `git status`: app sigue untracked; HEAD `976f757`. Los cambios de IMPL-01/02/03 no están en git; la reversión depende del reporte IMPL. **Requiere decisión Frank/INTEGRA antes de cualquier despliegue.** |

**Totales este informe:** P0: 0 · P1: 0 · P2: 0 · P3: 0 nuevos. 3 P3 previos CERRADOS + 1 ACEPTADO sin regresión.

---

## E. Riesgo operativo

- **Seguridad:** sin secretos en el incremento (4 archivos de código/tests revisados; ningún `.env` tocado). La compensación sigue usando el cliente con cookies → RLS acota los deletes al tenant, además de los filtros por IDs del intento. Escape HTML de la plantilla intacto (tests XSS PASS). Sin dependencias nuevas.
- **Datos:** sin migraciones; sin schema tocado. Con N1 cerrado, **todos** los paths de fallo post-insert de `duplicarPlaneacion` compensan (cero filas huérfanas ante error de DB). El booleano exacto de N3-ii hace observable el caso patológico de fallo de compensación (mensaje verbatim al usuario + `console.warn` por tabla con `planeacion_id`).
- **Operabilidad:** cero chromium huérfanos verificado en entorno real, incluyendo el modo de fallo de `newPage()` (N2). Cada request sigue pagando el lanzamiento del browser (~1.8 s en esta corrida); caché diferido (ADR decisión 3).
- **Observabilidad:** mejora respecto a QA-02: warns de fallo por delete individual (bloque/sesion/planeacion) con `planeacion_id` + tabla, y mensaje de error al usuario que orienta la limpieza manual en el caso patológico.
- **Reversión:** documentada en el reporte IMPL (4 pasos, sin schema). No ejecutada ni autorizada. Condicionada por P3-5 (sin commits).

---

## F. Preparación por entorno

- **Calidad del incremento:** **PASS** — AC-P3-1..AC-P3-6 y AC-G1..AC-G4 verificados independientemente (todos PASS); 0 findings nuevos; 3 P3 cerrados + 1 aceptado sin regresión; contratos §6.6/§6.30 y protegidos intactos.
- **Staging:** **LISTO** — build y tests PASS; mismas condiciones que QA-01/02: entorno destino con `PDF_GENERATOR=playwright` + chromium (o el endpoint degrada a 422 seguro). Los riesgos nominales restantes (causa (b) del hash, compensación no transaccional) están aceptados por ADR-20260819-01. E2E (P3-2) sigue siendo gate de promoción real.
- **Producción:** **NO_LISTO** — pendiente: (i) E2E T-E2E-05 + flujo D-FIN-17 contra entorno real; (ii) estrategia de commits (P3-5, decisión Frank); (iii) cierre total `ARCH-20260819-02` (Storage + RPC) o aceptación explícita de los nominales; (iv) OK explícito de Frank para cualquier despliegue.

---

## G. Handoff a INTEGRA

**Acción requerida:** INTEGRA recibe el veredicto **PASS** y decide la transición de la unidad `VERIFYING → DONE` (los gates de calidad del incremento están completos: SOFIA `READY_FOR_VERIFYING` + GEMINI PASS; P3-N4 con aceptación documentada vigente). Recomendado:

1. Marcar `DONE` la unidad P3-FIXES-L1 y registrar que los P3 de QA-02 quedaron cerrados (N1/N2/N3) o aceptados (N4).
2. Mantener el seguimiento de residuales: causa (b) del hash + compensación no transaccional → `ARCH-20260819-02`; P3-1/P3-2/P3-4 según QA-01.
3. Resolver P3-5 (estrategia de commits) con Frank antes de cualquier despliegue; E2E T-E2E-05 sigue siendo gate de staging/producción real.

GEMINI no mueve el estado global ni envía handoff `DONE` a CRONISTA: la transición la aplica INTEGRA (vía CRONISTA si corresponde).

---

## Autoauditoría GEMINI

- [x] Delimité el incremento exacto (find global + mtimes 19:15–19:23; limitación git documentada; inventario coincide 1:1 con el reporte IMPL).
- [x] Verifiqué SPEC/ADR vigentes (ADR-20260819-01 + addendum, handoff P3-FIXES, handoff P2-FIXES Fix 2 punto 4, §6.6/§6.30, D-FIN-5/D-FIN-17).
- [x] Evidencia independiente: re-ejecuté typecheck, lint, suite completa, re-ejecución verbose de 33 tests, build, smoke chromium, `file`/`sha256sum`/`od`/`strings`, `ps` post-smoke, greps de verbatim/no-export/independencia/`new Date()`/`updated_at`/consumidores.
- [x] Traté el reporte de SOFIA como índice: cada AC verificado contra código y ejecución propia; no detecté desviaciones (0 findings nuevos).
- [x] No edité código, tests, config, discovery, SPEC ni PROYECTO.md; sólo creé este reporte.
- [x] No imprimí secretos ni PII.
- [x] Separé severidad QA (P0-P3) de niveles de fix (L1 ya ejecutados por SOFIA).
- [x] Separé calidad del incremento, staging y producción.
- [x] No invoqué subagentes ni declaré `DONE`.
- [x] Handoff a INTEGRA con acción concreta.
