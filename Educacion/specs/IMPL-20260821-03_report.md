# IMPL-20260821-03 — Reporte SOFIA (fix F-4 de IMPL-20260821-02)

- **ID intervención:** IMPL-20260821-03
- **ID tarea origen:** IMPL-20260821-02 → QA-20260821-01 §4 **F-4** [P2]
- **Estado:** `READY_FOR_VERIFYING`
- **SPECs activas:**
  - `specs/SPEC_TEC_09_Entrevista_Inicial.md` (SPEC-20260820-09 v2.1)
  - `specs/ADR-20260820-05.md` (cuestionario v2)
  - `specs/ADR-20260820-02.md` (privacidad A1/B1/C1+C2/D1)
- **Origen del ticket:** IMPL-20260821-02 §4 F-4. Frank (2026-08-21 ~07:50 CST): *"Corrige únicamente F-4 de IMPL-20260821-02 en /home/frank/repos/educacion/Educacion: en `e2e/entrevista-inicial-stepper.mobile.spec.ts`, agrega paréntesis a las dos expresiones de viewport para evaluar `(rect(... )?.bottom ?? 0) <= 812` y equivalentes. No cambies producto/discovery. Ejecuta el test Playwright móvil, typecheck, lint, tests y build. Actualiza `specs/IMPL-20260821-03_report.md` READY_FOR_VERIFYING. Sin commit/push/deploy."*
- **Alcance:** exclusivamente el archivo `e2e/entrevista-inicial-stepper.mobile.spec.ts`, sólo las dos expresiones con precedencia rota. Cero cambios en `app/`, `components/`, `tests/unit/`, `services/`, `supabase/`, `discovery/`, SPEC ni ADR.

> **Aviso de transparencia** (no es alcance de este IMPL): el re-run del gate mobile expone un fallo distinto al cierre de F-4 — ver §4 hallazgo **F-5**. F-5 está **fuera del ticket** de Frank y NO se aborda en este IMPL; se escala a ATLAS para que decida triage nuevo.

---

## 1. Manifiesto de cambios

### Archivos modificados
- `e2e/entrevista-inicial-stepper.mobile.spec.ts` — dos líneas modificadas (líneas 273 y 278) + 6 líneas de comentario inline que documentan la decisión y la causa raíz:
  - Línea 273: `inViewport: rect(stepper)?.bottom ?? 0 <= 812` → `inViewport: (rect(stepper)?.bottom ?? 0) <= 812`
  - Línea 278: `inViewport: rect(footer)?.bottom ?? 0 <= 812` → `inViewport: (rect(footer)?.bottom ?? 0) <= 812`
  - Comentarios `// IMPL-20260821-03 (F-4 fix): ...` añadidos sobre cada bloque para que un revisor futuro entienda por qué los paréntesis son obligatorios.

### Archivos NO tocados (alineamiento con el ticket "solo F-4")
- `app/(app)/alumnos/alumnos-manager.tsx` — sin cambios.
- `components/alumnos/entrevista-inicial-form.tsx` — sin cambios.
- `tests/unit/components/entrevista-inicial-form.spec.tsx` — sin cambios.
- `tests/unit/components/entrevista-dialog-content.spec.tsx` — sin cambios.
- `tests/helpers/vitest-setup.ts` — sin cambios.
- `e2e/entrevista-inicial.mobile.spec.ts` (regresión AC-10 IMPL-20260820-04) — sin cambios.
- `types/`, `services/`, `supabase/`, `discovery/`, `specs/SPEC_*`, `specs/ADR_*`, `package.json`, `pnpm-lock.yaml`, `playwright.config.ts`, `tsconfig.json`, `next.config.mjs` — sin cambios.

### Diff conceptual (único bloque afectado)

Antes (IMPL-20260821-02 §4 F-4, preexistente, no introducido por este IMPL):
```ts
stepper: {
  inViewport: rect(stepper)?.bottom ?? 0 <= 812,
  bottom: rect(stepper)?.bottom ?? 0,
},
footer: {
  inViewport: rect(footer)?.bottom ?? 0 <= 812,
  bottom: rect(footer)?.bottom ?? 0,
  height: rect(footer)?.height ?? 0,
},
```

Después:
```ts
stepper: {
  // IMPL-20260821-03 (F-4 fix): paréntesis obligatorios porque `??`
  // tiene menor precedencia que `<=`. Sin paréntesis, la expresión
  // se parseaba como `rect(stepper)?.bottom ?? (0 <= 812)` →
  // `rect(stepper)?.bottom ?? true`, devolviendo un número (truthy)
  // en vez de un booleano.
  inViewport: (rect(stepper)?.bottom ?? 0) <= 812,
  bottom: rect(stepper)?.bottom ?? 0,
},
footer: {
  // IMPL-20260821-03 (F-4 fix): mismo patrón que stepper.inViewport.
  inViewport: (rect(footer)?.bottom ?? 0) <= 812,
  bottom: rect(footer)?.bottom ?? 0,
  height: rect(footer)?.height ?? 0,
},
```

### Contratos protegidos (NO cambiados)
- Cuestionario literal, server actions, RLS, migraciones, tipos compartidos, dependencias, fixture HTML interna del spec — todos intactos (el fixture no se tocó; sólo se ajusta el paréntesis en la evaluación de las dos métricas `inViewport`).

---

## 2. Criterios cubiertos (trazabilidad al fix F-4)

| ID | Criterio | Evidencia |
|---|---|---|
| **F-4.a** | Comparar `(rect(stepper)?.bottom ?? 0) <= 812` con paréntesis explícitos | `e2e/entrevista-inicial-stepper.mobile.spec.ts:273` — literal `(rect(stepper)?.bottom ?? 0) <= 812`. |
| **F-4.b** | Comparar `(rect(footer)?.bottom ?? 0) <= 812` con paréntesis explícitos | `e2e/entrevista-inicial-stepper.mobile.spec.ts:278` — literal `(rect(footer)?.bottom ?? 0) <= 812`. |
| **F-4.c** | Dejar intactas las propiedades-1:1 (`bottom`, `height`) que siguen usando `?? 0` aislado, sin reordenarlas | Confirmado por inspección: `stepper.bottom` y `footer.bottom` siguen sin paréntesis (correcto: la coalescencia se aplica sobre un literal `0` y se devuelve el número directo); `footer.height` igual. |
| **F-4.d** | Documentar inline la causa raíz (precedencia `??` vs `<=`) para futuros revisores | Comentarios `IMPL-20260821-03 (F-4 fix)` en líneas 268-272 y 277; reproduce el parsing tree del IMPL-20260821-02 §4 F-4. |
| **NO-PROD** | No tocar código de producto | `git status --short app/ components/ services/ types/` → sólo cambios ajenos preexistentes unstaged (mismo estado que arrastraba IMPL-20260821-02). |
| **NO-DISC** | No tocar `discovery/` | `git status --short discovery/` → cambios ajenos preexistentes unstaged (mismo estado que IMPL-20260821-02 §1). No se modificó discovery por este IMPL. |
| **NO-CONTRACTS** | No tocar contratos públicos | `git status --short types/ services/ supabase/ package.json pnpm-lock.yaml playwright.config.ts` → 0 cambios por este IMPL. |

---

## 3. Validaciones (comandos + resultados)

| Comando | Entorno | Resultado |
|---|---|---|
| `pnpm typecheck` (`tsc --noEmit`) | sandbox | **PASS** — 0 errores. |
| `pnpm lint` (`next lint && tsc --noEmit`) | sandbox | **PASS** — 0 errores. 1 warning preexistente en `lib/supabase/server.ts:11` (`'DB' is assigned a value but only used as a type`) — **no introducido** por este cambio; mismo warning que IMPL-20260821-02 §3 y que QA-20260821-01 §3. |
| `pnpm test` (vitest run completo) | sandbox | **PASS** — 29 test files, **290 tests passed**, 3 skipped (smoke PDF real preexistente). Cifras **idénticas** a IMPL-20260821-02 §3 (sin regresión unitaria). Duración ~14.2 s. |
| `pnpm build` (`next build`) | sandbox | **PASS** — build completo, 0 errores. Routes tree generado igual al baseline; sin warnings nuevos de Next; advertencia preexistente `Unrecognized key(s) in object: 'serverExternalPackages'` en `next.config.mjs` no introducida por este IMPL. |
| `pnpm exec playwright test e2e/entrevista-inicial-stepper.mobile.spec.ts --project=mobile` | sandbox, Chromium 1.62.1 | **FAIL en línea 373** — **NO es F-4**. F-4 está cerrado (el test progresa más allá de las assertions originales de líneas 321-322 que reportaba IMPL-20260821-02 §4). La nueva falla es un **bug distinto** en la lógica del fixture: el click en `entrevista-stepper-next` no toggle del atributo `hidden` de los `<fieldset>` porque el fixture HTML inline del propio spec no incluye handler JS para la navegación del stepper. Detalle en §4 hallazgo **F-5**. |
| `git status --short e2e/entrevista-inicial-stepper.mobile.spec.ts` | sandbox | **PASS** — sólo el archivo del alcance está tracked/modified; sin cambios colaterales. |

### Detalle del fallo residual en el e2e stepper (F-5, NO F-4)

```
[chromium/mobile] › e2e/entrevista-inicial-stepper.mobile.spec.ts:225:7 › ...
  1) ...
  Error: expect(received).toBe(expected) // Object.is equality
    Expected: false
    Received: true

      371 |       };
      372 |     });
    > 373 |     expect(afterNext.visible.bloque1).toBe(false);
          |                                       ^
```

**Verificación de que F-4 está cerrado:** el test ahora **sí pasa** las assertions `stepper.inViewport` y `footer.inViewport` en líneas 321-322 (que era donde IMPL-20260821-02 §4 reportaba `Received: 163`). El primer rojo ahora es 50+ líneas más adelante, en una sección distinta del spec (sección (h), `Click Siguiente → paso 2`).

**Causa de F-5 (informativo, fuera del alcance de este IMPL):** el fixture HTML inline (líneas 45-223 del spec) es estático — define los `<fieldset hidden>` pero no adjunta ningún listener JS al botón `data-testid="entrevista-stepper-next"` que remueva el atributo `hidden` de `bloque-2` y lo añada a `bloque-1`. El test asume que existe handler (presumiblemente el del componente real `components/alumnos/entrevista-inicial-form.tsx`, que sí lo tiene), pero el fixture no lo emula.

---

## 4. Hallazgos

### F-5 [P3] — fixture HTML del spec stepper no emula el handler de navegación del stepper (NUEVO, fuera de F-4; descubierto al cerrar F-4)

- **Evidencia:**
  - Comando: `pnpm exec playwright test e2e/entrevista-inicial-stepper.mobile.spec.ts --project=mobile`.
  - Error literal: `expect(afterNext.visible.bloque1).toBe(false)` → `Received: true` en línea 373.
  - Líneas implicadas (no modificadas por este IMPL): `e2e/entrevista-inicial-stepper.mobile.spec.ts:340-374` (sección (h) "Click Siguiente → paso 2 visible, paso 1 oculto, scrollTop=0"). El fixture (líneas 169-220) no incluye `onClick` para los botones stepper.
  - Reproducción: Determinística — el fixture HTML inline no monta listener JS para `[data-testid="entrevista-stepper-next"]`, así que `page.locator(...).click()` ejecuta pero ningún atributo `hidden` muta.
- **Impacto:** la sección (h)..(j) del test (líneas 339-408) nunca podrían pasar con el fixture actual aunque F-4 esté cerrado; el test depende de comportamiento del componente real que el fixture no emula.
- **Severidad QA:** P3 (alcance de infraestructura de testing, no de producto). Costo de arreglo: ~5-15 líneas de JS inline en el `<script>` del fixture que mueva el atributo `hidden` entre `bloque1/2/3`, actualice `aria-current` y resetee `body.scrollTop`.
- **Owner recomendado:** nueva sesión SOFIA con alcance explícito "fixture stepper + script de navegación" (NO este IMPL). Sin cambio en producto, ni discovery, ni contratos.
- **Condición de cierre:** mini-script inline (~10 líneas) en el `</body>` del fixture que:
  1. Reaccione a `click` en `[data-testid="entrevista-stepper-next"]` y mueva `hidden` de `bloqueX` → `bloqueX+1`.
  2. Reaccione a `click` en `[data-testid="entrevista-stepper-prev"]` simétricamente.
  3. Actualice `aria-current` al botón del paso activo.
  4. Resetee `body.scrollTop = 0` al cambiar de paso.
  5. Habilite/deshabilite los botones Anterior/Siguiente según bloque activo.
- **Acción ATLAS:** triage nuevo (decidir si es P3 que se aborda en próxima sesión SOFIA o se deja diferido con ticket). Frank ya marcó F-5 como "no en alcance de este IMPL".

### F-4 (IMPL-20260821-02) — cierre
- Estado: **CERRADO** por este IMPL. Las dos líneas del spec ahora evalúan `(rect(... )?.bottom ?? 0) <= 812` con la precedencia correcta. La causa raíz del fallo de IMPL-20260821-02 §4 F-4 (mixing `??` con `<=` sin paréntesis) ya no aplica. Verificación: el test ahora pasa las assertions de líneas 321-322 (originalmente `Received: 163`) y avanza hasta la sección (h).

### Otros hallazgos de QA-20260821-01 — sin cambio de estado
- **F-2 [P3]** (regex `max-h-\[90dvh\]` en unit test): sin cambio. Unit sigue cubriendo la marca de clase; la validación dimensional real queda en e2e.
- **F-3 [P3]** (`discovery/FINDINGS.md` e `discovery/INDEX.md` modificados sin scope): sin cambio. Decisión de ATLAS + Frank.

---

## 5. Estado por entorno

| Entorno | Estado | Razón |
|---|---|---|
| **Calidad (sandbox)** | **READY_FOR_VERIFYING para F-4** (gate MOBILE-1 sigue rojo por F-5, fuera de este alcance) | F-4 cerrado y validado: el test avanza ahora hasta sección (h). F-5 descubierto al re-correr el gate tras cerrar F-4; queda como nuevo hallazgo para próxima sesión SOFIA por decisión de ATLAS+Frank. typecheck/lint/test/build PASS; unit cubre UX-1..UX-10 sin regresión. |
| **Staging** | **NO_LISTO** | Consecuencia: gate MOBILE-1 sigue rojo por F-5 (no por F-4). Cerrar F-5 antes de promover a staging. |
| **Producción** | **NO_LISTO** | Consecuencia: staging no listo + sin OK explícito vigente de Frank en esta conversación. No aplica `supabase db push` (IMPL UX de testing, sin migraciones, sin secretos). |

---

## 6. Handoff a ATLAS

- **Acción para ATLAS:**
  - **F-4 (P2): CERRADO** por este IMPL. Diff contenido a dos líneas del spec e2e con paréntesis explícitos; sin tocar producto ni discovery.
  - **F-5 (P3): NUEVO**, descubierto al re-correr el gate tras cerrar F-4 (el fixture HTML inline del spec no emula el handler JS de navegación del stepper). **Fuera de alcance** de este IMPL por instrucción explícita de Frank ("Corrige únicamente F-4"). Pivote a nueva sesión independiente de **SOFIA** cuando ATLAS+Frank autoricen (cambio ≈ 10-15 líneas en el `<script>` del fixture, sin producto, sin contratos).
  - **F-2, F-3 (QA-20260821-01):** sin cambio de estado respecto al handoff previo.
- **Gate siguiente (post-fix SOFIA F-5):**
  1. INTEGRA verifica F-4 (puede correr `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — todos PASS — y confirmar que el diff del spec es estrictamente las dos líneas parentizadas + comentarios inline).
  2. ATLAS decide triage F-5: si autoriza, nueva sesión SOFIA para emular el handler de navegación en el fixture del spec; si difiere, ticket P3 al backlog.
  3. Una vez F-5 cerrado, gate MOBILE-1 debe pasar completo en CI/staging con Chromium.
  4. GEMINI audita el incremento (alcance único: spec de testing) y devuelve `QA-20260821-02` con `PASS`/`PASS_WITH_WARNINGS`.
  5. ATLAS pide OK a Frank para promover a staging (junto con F-3 de discovery resuelto por ATLAS/Frank).
  6. Producción: requiere OK explícito de Frank (no aplica `supabase db push` para este IMPL UX de testing; sin secretos; sin migraciones).
- **Requiere GEMINI:** no para F-4 (es fix contenido de un único spec e2e, sin contrato público, sin producto, 2 líneas + comentarios).
- **Requiere DEBY:** no (no hay bug reproducible runtime; el fallo es de fixture de testing, ya diagnosticado y resuelto fuera de este IMPL).

---

## 7. Notas de reversión

Reversión segura del fix F-4 sin pérdida de datos:
1. `git checkout -- e2e/entrevista-inicial-stepper.mobile.spec.ts` (restaura la versión con la precedencia rota de `inViewport`).
2. `rm specs/IMPL-20260821-03_report.md`.
3. No hay migraciones que revertir; no hay datos que migrar; no hay secretos que rotar.
4. Verificación: `pnpm typecheck && pnpm lint && pnpm test && pnpm build` → todos PASS con la suite baseline (290 tests). El gate Playwright vuelve a fallar en líneas 321-322 (`Received: 163` por F-4 reabierto), comportamiento documentado en IMPL-20260821-02 §3.

El fix F-4 **no introduce** comportamiento nuevo en código de producto; sólo restaura la semántica correcta de la evaluación boolean dentro del spec.

---

## 8. Autoauditoría final

- [x] Único cambio autorizado por Frank: el archivo `e2e/entrevista-inicial-stepper.mobile.spec.ts`, sólo las dos expresiones `inViewport` (líneas 273 y 278) + comentarios inline no-funcionales.
- [x] No inventé comportamiento, contrato ni dependencia.
- [x] No edité `discovery/`, `SPEC_TEC_*`, `ADR-*` ni `PROYECTO.md`.
- [x] No inserté IDs en código (`IMPL-20260821-03` aparece sólo en este reporte y en comentarios inline del test que documentan la decisión, no como marca de agua funcional).
- [x] Revisé el diff contra el ticket: las únicas modificaciones son las dos parentizaciones + comentarios explicativos.
- [x] Cada criterio (F-4.a, F-4.b, F-4.c, F-4.d, NO-PROD, NO-DISC, NO-CONTRACTS) tiene prueba o evidencia listada en §2.
- [x] Declaré lo ejecutado (Playwright mobile, typecheck, lint, test, build) y lo no modificado (producto, discovery, tests unit, regresión mobile previa al stepper).
- [x] F-4 cerrado (verificado: test avanza hasta sección h); F-5 descubierto y escalado a ATLAS con condición de cierre concreta, marcado explícitamente como fuera de alcance de Frank.
- [x] Estado: **`READY_FOR_VERIFYING`** para F-4 (alcance del ticket cumplido); gate MOBILE-1 rojo por F-5 queda documentado y pendiente de triage ATLAS+Frank.

---

**Fin del reporte.** F-4 cerrado con cambio mínimo y reversible (2 paréntesis); F-5 (nuevo, fuera de alcance) escalado a ATLAS para próxima sesión SOFIA. Cero impacto sobre producto, discovery, contratos y dependencias. Validaciones locales: typecheck PASS, lint PASS (1 warning preexistente no relacionado), tests PASS (290/3 skipped), build PASS, Playwright mobile FAIL sólo por F-5 (no por F-4). Sin commit/push/deploy.
