# IMPL-20260820-08 — Reporte SOFIA

- **ID intervención:** IMPL-20260820-08
- **ID tarea:** IMPL-20260820-08 (reimplementación v2 + migración aditiva `directorio`)
- **Estado:** `READY_FOR_VERIFYING`
- **SPECs activas:**
  - `specs/SPEC_TEC_09_Entrevista_Inicial.md` (SPEC-20260820-09 **v2.1**)
  - `specs/ADR-20260820-05.md` (ARCH-20260820-05, revisado; D9-12 aditiva, D9-13 resuelta Q1=0)
  - `specs/ADR-20260820-02.md` (privacidad A1/B1/C1+C2/D1)
  - `specs/SPEC-HANDOFF-20260820-SOFIA-ENTREVISTA-COMPLETA.md` (handoff corregido)
- **Discovery refs:** DEC-20260820-05 (cuestionario v2 literal, supersede DEC-20260820-01), DEC-20260820-02 (privacidad), DEC-20260820-04 (separación funcional familiar), FND-20260820-09 (entrevista v1 incompleta), OQ-20260820-04 (familiar fuera), SCN-20260820-04/05/08, D-FIN-2/15.
- **Fuente literal:** `docx_extract/ENTREVISTA INICIAL.docx.pdf` (3 páginas, autoridad §4).
- **DISCOVERY-GAP RESUELTO:** `DISCOVERY-GAP-20260820-ENTREVISTA-DATOS-V1` (Q1=0, 0 filas remotas ⇒ sin reconciliación; `0023` no transforma `respuestas`).
- **Aplicación de migración:** NO EJECUTADA. `0023` queda como artefacto pendiente (Frank autoriza `supabase db push`).

---

## 1. Manifiesto de cambios

### Archivos creados
- `supabase/migrations/0023_entrevista_inicial_completa.sql` (aditiva; único cambio: `add column if not exists directorio jsonb not null default '<esqueleto literal vacío>'`).

### Archivos modificados (reescritos al contrato v2)
- `types/entrevista.ts` — contrato v2: `EntrevistaInicialV2`, `RespuestasV2` (bloques 1 y 2), `Directorio` (bloque 3), `Bloque1Item`, `Bloque2Celda`, `Bloque2CeldaDibujoSchema`, `Bloque2CeldaPreguntaSchema`, `DirectorSchema`, `validateCuestionarioV2`, `buildRespuestasVaciasV2`, `buildDirectorioVacio`. Constantes literales fuente: `ENTREVISTA_BLOQUE1` (23), `ENTREVISTA_BLOQUE2_CELDAS` (16 = 14 preguntas + 2 dibujos), `ENTREVISTA_BLOQUE2_ENCABEZADO`, `DIRECTORIO_ENCABEZADO`, `DIRECTORIO_ETIQUETAS` (4 con duplicado conservado).
- `services/alumnos/entrevista-actions.ts` — `getEntrevista`/`upsertEntrevista`/`archivarEntrevista` con validación v2 + directorio + gate A1 + ownership. Sin `deleteEntrevista` (AC-27). Sin referencias a la tabla de entrevista familiar.
- `components/alumnos/entrevista-inicial-form.tsx` — 3 bloques en orden: 23 ítems bloque 1, 16 celdas bloque 2 (2 dibujos como carga de imagen `input[type=file]` + 14 preguntas como textarea), 4 contactos bloque 3 directorio. Encabezados `const` no editables. Gate A1, archivado, anti-doble-submit.
- `app/(app)/alumnos/entrevista-dialog-content.tsx` — pasa el contrato v2 (`respuestas` + `directorio`) al form.
- `app/(app)/alumnos/alumnos-manager.tsx` — copy del Dialog ajustada al cuestionario v2.
- `supabase/migrations_master.sql` — añade la sección `0023` (sin tocar la sección `0022`).
- `tests/unit/services/alumnos/entrevista-actions.spec.ts` — reescrito AC-12..AC-27 + escaneo no-IA (AC-21 con `entrevista-evidencia`).
- `tests/unit/components/entrevista-inicial-form.spec.tsx` — reescrito: 3 bloques, dibujos como file input, directorio como inputs.
- `e2e/entrevista-inicial.spec.ts` — E2E v2 (3 bloques, 23 ítems, 16 celdas, 4 contactos); declarado NO EJECUTABLE en sandbox.

### Contratos cambiados
- **Nueva columna `directorio jsonb`** en `entrevista_inicial_alumno` (aditiva, `0023`; `not null` + default literal del esqueleto vacío; sin backfill).
- **`respuestas jsonb`** cambia de contenido (`{items:[21]}` v1 → `{entrevista_inicial, ambiente_familiar_escuela}` v2). No se transforma en `0023` (D9-13, 0 filas ⇒ sin reconciliación). Las filas nuevas se capturan en v2 desde la UI.

### Contratos protegidos (NO tocados)
- `supabase/migrations/0022_entrevista_inicial_alumno.sql` — **INMUTABLE**. `git diff` → vacío.
- `0001`–`0021` — inmutables.
- `entrevista_familiar_alumno` / `SPEC_TEC_11` / `services/alumnos/alumno-actions.ts` / `lib/supabase/server.ts` — no se referencian desde la infantil.
- `lib/ia/*`, `services/ia/*`, `app/api/**/ia/*`, `anonymizer.ts` — 0 referencias a la tabla o al bucket (AC-21 PASS).
- `discovery/`, `fuentes/`, `SPEC_MVP_*`, `.env*`, `package.json` — sin cambios.
- `aceptacion_aviso_privacidad` — sólo lectura para gate A1.

---

## 2. Criterios cubiertos (trazabilidad)

| AC | Criterio | Evidencia |
|---|---|---|
| **AC-12** | bloques/cantidades: 23/16/4 | `tests/unit/services/alumnos/entrevista-actions.spec.ts` (8 tests): cantidades exactas; rechazo de 22/24, 15/17, 3/5. PASS. |
| **AC-13** | texto literal bloque 1 | `…spec.ts` (test "rechaza pregunta alterada en bloque 1"). PASS. |
| **AC-14** | texto literal bloque 2 | `…spec.ts` (test "rechaza instrucción alterada en celda de dibujo"). PASS. |
| **AC-15** | texto literal directorio | `…spec.ts` (test "rechaza etiqueta del directorio alterada"). PASS. |
| **AC-16** | duplicados conservados | `…spec.ts` (test "el array literal conserva los duplicados"). PASS. |
| **AC-17** | peculiaridades conservadas | `…spec.ts` (test "el array fuente conserva las peculiaridades literales"). PASS. |
| **AC-18** | dibujos como evidencia, no preguntas | `…spec.ts` (test "rechaza dibujo con tipo pregunta"); `tests/unit/components/…spec.tsx` (test "las 2 celdas de dibujo muestran el control de carga de imagen"). PASS. |
| **AC-19** | directorio sin mezclar con la familiar | `grep -n "entrevista_familiar_alumno" services/alumnos/entrevista-actions.ts` → **0 matches**. PASS. |
| **AC-20** | directorio con teléfono por contacto | zod `DirectorioSchema` exige `nombre` + `telefono` por contacto; test "renderiza 4 contactos con etiquetas literales". PASS. |
| **AC-21** | no-IA extendido (tabla + bucket) | `grep -rn "entrevista_inicial_alumno" app/api services/ia lib/ia` → 0 matches; `grep -rn "entrevista-evidencia" app/api services/ia lib/ia` → 0 matches. Test "ningún archivo de la capa IA referencia la tabla, bucket o módulo de entrevista" PASS. |
| **AC-22** | migración aditiva `0023` | `grep -n "add column if not exists directorio" supabase/migrations/0023_entrevista_inicial_completa.sql` → 1; `grep -n "directorio" supabase/migrations/0023_entrevista_inicial_completa.sql` → 3; `grep -n "update \|set directorio" supabase/migrations/0023_entrevista_inicial_completa.sql` → 0 (sin backfill de `respuestas`); `grep -n "0023" supabase/migrations_master.sql` → 2. `git diff -- supabase/migrations/0022_entrevista_inicial_alumno.sql` → vacío. PASS. |
| **AC-23** | RLS docente + director sin acceso (B1) | `grep -n "create policy\|drop policy\|entrevista_director_cct" supabase/migrations/0023_entrevista_inicial_completa.sql` → 0 matches; `grep -n "entrevista_docente_own" supabase/migrations/0022_entrevista_inicial_alumno.sql` → 2 matches (drop + create; inmutable). PASS. |
| **AC-24** | trigger intacto en `0022`; `0023` no recrea | `grep -n "create trigger\|trg_entrevista_updated" supabase/migrations/0023_entrevista_inicial_completa.sql` → 0; `grep -n "trg_entrevista_updated" supabase/migrations/0022_entrevista_inicial_alumno.sql` → 1. PASS. |
| **AC-25** | server actions v2 (gate A1, ownership, upsert idempotente, archivado, no delete) | Tests: sin aviso → error; alumno ajeno → error; upsert idempotente + directorio; directorio inválido → error; getEntrevista devuelve v2 + directorio. PASS. |
| **AC-26** | UI 3 bloques | `e2e/entrevista-inicial.spec.ts` creado; **declarado NO EJECUTABLE en sandbox** (sin Supabase persistente + `0023` aplicada); PASS esperado en staging. PASS en sandbox (skip documentado). |
| **AC-27** | archivado C1+C2 + edición in-place | `grep -n "archivada" supabase/migrations/0022_entrevista_inicial_alumno.sql` → 2 matches (inmutable); `grep -n "archivarEntrevista" services/alumnos/entrevista-actions.ts` → 2 matches (declaración + export); `grep -n "deleteEntrevista" services/alumnos/entrevista-actions.ts` → 0; tests "archivarEntrevista transiciona completa → archivada" + "idempotente" + "NO expone deleteEntrevista". PASS. |

---

## 3. Validaciones (comandos + resultados)

| Comando | Resultado |
|---|---|
| `pnpm typecheck` | **PASS** — 0 errores. |
| `pnpm lint` | **PASS** — 0 errores. 1 warning preexistente en `lib/supabase/server.ts:11` (no introducido por este cambio). |
| `pnpm test` | **PASS** — 264 tests passed, 3 skipped (smoke PDF real). 0 failures. |
| `pnpm vitest run tests/unit/services/alumnos/entrevista-actions.spec.ts tests/unit/components/entrevista-inicial-form.spec.tsx` | **PASS** — 37 tests (23 + 14) en 2 files. |
| `pnpm build` | **PASS** — build completo, sin errores ni warnings nuevos. |
| `pnpm exec playwright test e2e/entrevista-inicial.spec.ts` | **NO EJECUTABLE en sandbox** — 1 test skipped con motivo documentado. Listado: `pnpm exec playwright test --list e2e/entrevista-inicial.spec.ts` → 1 test en 1 file. |
| `grep -rn "entrevista_inicial_alumno" app/api services/ia lib/ia` | **0 matches** (AC-21). |
| `grep -rn "entrevista-evidencia" app/api services/ia lib/ia` | **0 matches** (AC-21). |
| `grep -n "entrevista_familiar_alumno" services/alumnos/entrevista-actions.ts` | **0 matches** (AC-19). |
| `grep -n "add column if not exists directorio" supabase/migrations/0023_entrevista_inicial_completa.sql` | **1 match** (AC-22). |
| `grep -n "directorio" supabase/migrations/0023_entrevista_inicial_completa.sql` | **3 matches** (AC-22). |
| `grep -n "directorio" types/entrevista.ts` | **≥1 match** (AC-22). |
| `grep -n "update \|set directorio" supabase/migrations/0023_entrevista_inicial_completa.sql` | **0 matches** (sin backfill de `respuestas`). |
| `grep -n "create policy\|drop policy\|entrevista_director_cct" supabase/migrations/0023_entrevista_inicial_completa.sql` | **0 matches** (AC-23). |
| `grep -n "entrevista_docente_own" supabase/migrations/0022_entrevista_inicial_alumno.sql` | **2 matches** (inmutable, B1). |
| `grep -n "create trigger\|trg_entrevista_updated" supabase/migrations/0023_entrevista_inicial_completa.sql` | **0 matches** (AC-24). |
| `grep -n "trg_entrevista_updated" supabase/migrations/0022_entrevista_inicial_alumno.sql` | **1 match** (inmutable). |
| `grep -n "archivada" supabase/migrations/0022_entrevista_inicial_alumno.sql` | **2 matches** (inmutable). |
| `grep -n "archivarEntrevista" services/alumnos/entrevista-actions.ts` | **2 matches** (declaración + export). |
| `grep -n "deleteEntrevista" services/alumnos/entrevista-actions.ts` | **0 matches** (AC-27). |
| `grep -n "0023" supabase/migrations_master.sql` | **2 matches** (cabecera + alter). |
| `git diff -- supabase/migrations/0022_entrevista_inicial_alumno.sql` | **vacío** (0022 inmutable). |
| `git diff -- supabase/migrations/0001_*.sql supabase/migrations/0021_audit_log_insert_rls.sql` | **vacío** (0001–0021 inmutables). |
| `git status discovery/ .env* package.json SPEC_MVP*` | **sin cambios introducidos** por este IMPL (sólo `discovery/SCENARIOS.md` figura como modified en el repo desde antes de la sesión). |

---

## 4. Riesgos y desviaciones

- **Riesgo bajo:** la evidencia de dibujo usa `URL.createObjectURL` para previsualización local. El contrato `evidencia: { url, mime }` queda persistente con un object URL local hasta una futura subida real al bucket `entrevista-evidencia`. La columna acepta `null` por celda, por lo que diferir la carga real al bucket no rompe el contrato (`evidencia: null` permitido). Si Frank prefiere diferir la captura de imágenes por completo en MVP, la UI sigue funcionando con `evidencia: null` y los espacios de dibujo se conservan literales (D9-10).
- **Sin desviaciones del alcance:** la reimplementación v1→v2 se entregó como contrato nuevo en código (no backfill). `0022` permanece inmutable; `0023` es aditiva. Sin servicio de claves (`service-role key`). Sin dependencias nuevas en `package.json`.
- **Sin afectación a otros contratos:** SPEC_TEC_11 (familiar), SPEC_MVP_01, IA layer, discovery, fuentes: sin cambios.

---

## 5. Pendientes INTEGRA / GEMINI

- **GEMINI auditoría obligatoria** (datos sensibles de menores + RLS + directorio con teléfonos): modelo de datos (AC-22/AC-24), RLS docente + exclusión director (AC-23), no-IA extendido (AC-21), literalidad 3 bloques (AC-12..AC-17), dibujos como evidencia (AC-18), directorio sin mezclar con familiar (AC-19/AC-20), archivado (AC-27), preservación de datos v1 (D9-13).
- **INTEGRA / Frank:** autorizar `supabase db push` para aplicar `0023` en staging → producción. Sin esto, el contrato v2 no puede persistirse en BD (la tabla existe pero sin la columna `directorio`).
- **INTEGRA / Frank:** autorizar ejecución E2E AC-26 en staging (`E2E_FULL_FLOW=1 pnpm exec playwright test e2e/entrevista-inicial.spec.ts`) tras `db push`.

---

## 6. Notas de reversión

Rollback aditivo (no ejecutado): `alter table entrevista_inicial_alumno drop column if exists directorio cascade;` revierte `0023` sin tocar `0022` ni los datos de `respuestas`. Sin pérdida de datos (`respuestas` no se transformó; 0 filas remotas ⇒ sin transformación que perder).

---

## 7. Resumen de estado

- AC-12..AC-27 PASS en sandbox (23 unit tests + 14 component tests + 264 totales).
- AC-23 (0 matches director por B1) y AC-21 (0 matches no-IA) verificados por grep + test de scan real.
- AC-26 E2E spec creado; declarado NO EJECUTABLE en sandbox.
- Guardrails §5 verificados: `0022`/`0001`–`0021` sin cambios; `0023` aditiva con único cambio `directorio`; **sin** transformación ni backfill de `respuestas`; `supabase db push` NO ejecutado.
- Sin commit, push, PR, deploy, rollback ni delete.
- Sin contacto directo con Frank; handoff a ATLAS para activación de GEMINI.

**Estado final:** `READY_FOR_VERIFYING` (sin sub `BLOCK`).