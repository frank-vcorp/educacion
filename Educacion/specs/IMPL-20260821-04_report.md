# IMPL-20260821-04 — Reporte SOFIA (fix F-5 de IMPL-20260821-03)

- **ID intervención:** IMPL-20260821-04
- **ID tarea origen:** IMPL-20260821-03 §4 hallazgo **F-5** [P3]
- **Estado:** `READY_FOR_VERIFYING`
- **SPECs activas:**
  - `specs/SPEC_TEC_09_Entrevista_Inicial.md` (SPEC-20260820-09 v2.1)
  - `specs/ADR-20260820-05.md` (cuestionario v2)
  - `specs/ADR-20260820-02.md` (privacidad A1/B1/C1+C2/D1)
- **Origen del ticket:** IMPL-20260821-03 §4 F-5 escalado a ATLAS; Frank (2026-08-21 ~07:55 CST): *"Corrige F-5 de IMPL-20260821-03 en /home/frank/repos/educacion/Educacion. Solo en `e2e/entrevista-inicial-stepper.mobile.spec.ts`, agrega al fixture HTML el JavaScript mínimo que emule el comportamiento real del stepper: botones de pasos y Anterior/Siguiente deben alternar `hidden` en los bloques, actualizar `aria-current`, resetear scrollTop del cuerpo y actualizar disabled. No cambies producto. Ejecuta el Playwright mobile completo, regresión mobile existente, typecheck, lint, tests y build. Actualiza `specs/IMPL-20260821-04_report.md` READY_FOR_VERIFYING. Sin commit/push/deploy."*
- **Alcance:** exclusivamente el archivo `e2e/entrevista-inicial-stepper.mobile.spec.ts`, sólo el `<script>` inline añadido al fixture HTML y un comentario `<!-- ... -->` que documenta el cambio. Cero cambios en `app/`, `components/`, `tests/`, `services/`, `supabase/`, `discovery/`, SPEC ni ADR.

---

## 1. Manifiesto de cambios

### Archivos modificados
- `e2e/entrevista-inicial-stepper.mobile.spec.ts` — bloque `<script>` inline añadido antes del `</body>` del `FIXTURE_HTML` (líneas 222-284) que emula la navegación real del stepper. Comentario HTML `<!-- IMPL-20260821-04 (F-5 fix): ... -->` añadido justo antes del `<script>` para documentar el propósito del bloque. **Cero modificaciones** al resto del spec (los assertions siguen literales; el HTML del fixture sigue igual; los selectores `data-testid` siguen igual).

### Archivos NO tocados (alineamiento con el ticket "solo F-5 en fixture")
- `app/(app)/alumnos/alumnos-manager.tsx` — sin cambios.
- `app/(app)/alumnos/entrevista-dialog-content.tsx` — sin cambios.
- `components/alumnos/entrevista-inicial-form.tsx` — sin cambios (el handler real NO se tocó; el fixture del test ahora lo emula).
- `tests/unit/components/entrevista-inicial-form.spec.tsx` — sin cambios.
- `tests/unit/components/entrevista-dialog-content.spec.tsx` — sin cambios.
- `tests/helpers/vitest-setup.ts` — sin cambios.
- `e2e/entrevista-inicial.mobile.spec.ts` (regresión AC-10 IMPL-20260820-04) — sin cambios.
- `e2e/entrevista-inicial.spec.ts` — sin cambios.
- `types/`, `services/`, `supabase/`, `discovery/`, `specs/SPEC_*`, `specs/ADR_*`, `package.json`, `pnpm-lock.yaml`, `playwright.config.ts`, `tsconfig.json`, `next.config.mjs` — sin cambios.

### Diff conceptual (único bloque añadido al fixture)

Antes (IMPL-20260821-03, preexistente, sin handler JS de stepper):

```html
  </div>
</body>
</html>`;
```

Después (sólo el `<script>` añadido justo antes del `</body>`; el resto del fixture intacto):

```html
  </div>
  <!-- IMPL-20260821-04 (F-5 fix): emulación del handler de navegación
       del stepper en el fixture HTML inline. El componente real
       (components/alumnos/entrevista-inicial-form.tsx) reacciona a
       clicks sobre los botones de paso y sobre Anterior/Siguiente
       moviendo el atributo hidden entre los fieldset data-step,
       actualizando aria-current, reseteando scrollTop del cuerpo y
       actualizando el atributo disabled de los botones Anterior/Siguiente.
       Este script reproduce esa semántica sin tocar producto ni
       componentes. -->
  <script>
    (function () {
      var TOTAL = 3;
      function setActive(n) {
        for (var i = 1; i <= TOTAL; i++) {
          var b = document.querySelector('[data-testid="entrevista-bloque-' + i + '"]');
          if (b) {
            if (i === n) b.removeAttribute('hidden');
            else b.setAttribute('hidden', '');
          }
          var btn = document.querySelector('[data-testid="entrevista-stepper-step-' + i + '"]');
          if (btn) {
            if (i === n) {
              btn.setAttribute('aria-current', 'step');
              btn.setAttribute('data-active', 'true');
            } else {
              btn.removeAttribute('aria-current');
              btn.setAttribute('data-active', 'false');
            }
          }
        }
        var db = document.querySelector('[data-testid="entrevista-dialog-body"]');
        if (db) db.scrollTop = 0;
        var prev = document.querySelector('[data-testid="entrevista-stepper-prev"]');
        var next = document.querySelector('[data-testid="entrevista-stepper-next"]');
        if (prev) prev.disabled = (n <= 1);
        if (next) next.disabled = (n >= TOTAL);
        document.documentElement.setAttribute('data-stepper-active', String(n));
      }
      function current() {
        var v = document.documentElement.getAttribute('data-stepper-active');
        return v ? Number(v) : 1;
      }
      function wire() {
        for (var i = 1; i <= TOTAL; i++) {
          (function (step) {
            var btn = document.querySelector('[data-testid="entrevista-stepper-step-' + step + '"]');
            if (btn) btn.addEventListener('click', function () { setActive(step); });
          })(i);
        }
        var prev = document.querySelector('[data-testid="entrevista-stepper-prev"]');
        var next = document.querySelector('[data-testid="entrevista-stepper-next"]');
        if (prev) prev.addEventListener('click', function () {
          var a = current();
          if (a > 1) setActive(a - 1);
        });
        if (next) next.addEventListener('click', function () {
          var a = current();
          if (a < TOTAL) setActive(a + 1);
        });
      }
      wire();
    })();
  </script>
</body>
</html>`;
```

### Contratos protegidos (NO cambiados)
- Cuestionario literal, server actions, RLS, migraciones, tipos compartidos, dependencias, fixture HTML del stepper (estructura, `data-testid`, estilos inline, semántica inicial) — todos intactos. Sólo se añade un `<script>` que **replica** el comportamiento del componente real para que el fixture pueda ejecutar el flujo de clicks del spec.

---

## 2. Criterios cubiertos (trazabilidad al fix F-5)

| ID | Criterio (ticket Frank) | Evidencia |
|---|---|---|
| **F-5.a** | Botones de pasos `[data-testid="entrevista-stepper-step-N"]` deben alternar `hidden` en los bloques al hacer click | `setActive(n)` recorre los 3 bloques y aplica `removeAttribute('hidden')` al activo / `setAttribute('hidden', '')` a los demás (líneas 234-240 del spec tras el cambio). El listener se asocia en `wire()` (línea 267-269). |
| **F-5.b** | Botón `Anterior` (`[data-testid="entrevista-stepper-prev"]`) debe alternar `hidden` y `disabled` | Listener `prev.addEventListener('click', ...)` decrementa `current()` y llama `setActive(a-1)` (líneas 273-276). `setActive` aplica `hidden` por bloque y `prev.disabled = (n <= 1)` (línea 256). |
| **F-5.c** | Botón `Siguiente` (`[data-testid="entrevista-stepper-next"]`) debe alternar `hidden` y `disabled` | Listener `next.addEventListener('click', ...)` incrementa `current()` y llama `setActive(a+1)` (líneas 277-280). `setActive` aplica `hidden` por bloque y `next.disabled = (n >= TOTAL)` (línea 257). |
| **F-5.d** | Actualizar `aria-current` en el botón del paso activo (los demás sin `aria-current`) | `setActive` aplica `setAttribute('aria-current', 'step')` al activo y `removeAttribute('aria-current')` a los demás (líneas 243-249). Estado inicial coherente con el fixture (paso 1 ya tiene `aria-current="step"` en el HTML). |
| **F-5.e** | Resetear `scrollTop` del cuerpo del stepper al cambiar de paso | `setActive` hace `db.scrollTop = 0` donde `db` es `[data-testid="entrevista-dialog-body"]` (líneas 252-253). El assert `afterNext.bodyScrollTop).toBe(0)` (línea 380 del spec) valida este efecto. |
| **F-5.f** | Actualizar `disabled` en Anterior/Siguiente según bloque activo | `setActive` aplica `prev.disabled = (n <= 1)` y `next.disabled = (n >= TOTAL)` (líneas 256-257). Los asserts `afterNext.prevDisabled`/`afterNext.nextDisabled` y `afterStep3.nextDisabled` validan este efecto. |
| **F-5.g** | No cambiar producto | `git status --short app/ components/ services/ types/` → 0 cambios por este IMPL. |
| **F-5.h** | No cambiar discovery ni SPEC/ADR | `git status --short discovery/ specs/SPEC_* specs/ADR-*` → 0 cambios por este IMPL. |
| **F-5.i** | Sin commit/push/deploy | No se ejecutó ningún comando de mutación de git. |

---

## 3. Validaciones (comandos + resultados)

| Comando | Entorno | Resultado |
|---|---|---|
| `pnpm typecheck` (`tsc --noEmit`) | sandbox | **PASS** — 0 errores. Nota: el primer intento (con backticks sin escapar en el comentario HTML) falló con TS1005; corregido cambiando los acentos graves por texto plano (los backticks dentro del template literal TS deben estar escapados o eliminarse); segundo intento PASS. |
| `pnpm lint` (`next lint && tsc --noEmit`) | sandbox | **PASS** — 0 errores. 1 warning preexistente en `lib/supabase/server.ts:11` (`'DB' is assigned a value but only used as a type`) — **no introducido** por este cambio; mismo warning que IMPL-20260821-02 §3, IMPL-20260821-03 §3 y QA-20260821-01 §3. |
| `pnpm test` (vitest run completo) | sandbox | **PASS** — 29 test files, **290 tests passed**, 3 skipped (smoke PDF real preexistente). Cifras **idénticas** a IMPL-20260821-03 §3 (sin regresión unitaria). Duración ~15.2 s. |
| `pnpm build` (`next build`) | sandbox | **PASS** — build completo, 0 errores. Routes tree generado igual al baseline; sin warnings nuevos de Next; advertencia preexistente `Unrecognized key(s) in object: 'serverExternalPackages'` en `next.config.mjs` no introducida por este IMPL. |
| `pnpm exec playwright test e2e/entrevista-inicial-stepper.mobile.spec.ts --project=mobile` | sandbox, Chromium | **PASS** — 1 test passed (5.1 s). El test ahora avanza más allá de las assertions originales de líneas 321-322 (F-4 cerrado por IMPL-20260821-03) **y** completa las secciones (h)..(j) que validan la alternancia de bloques, `aria-current`, scrollTop y disabled (F-5 cerrado por este IMPL). |
| `pnpm exec playwright test e2e/entrevista-inicial.mobile.spec.ts --project=mobile` (regresión AC-10 IMPL-20260820-04) | sandbox, Chromium | **PASS** — 1 test passed (5.5 s). Sin regresión mobile. |
| `git status --short app/ components/ services/ tests/ supabase/ types/ discovery/ specs/` | sandbox | **PASS** — 0 cambios fuera del alcance del ticket; sólo `e2e/entrevista-inicial-stepper.mobile.spec.ts` modificado (untracked preexistente en la rama). |

### Comparación de resultados F-5 antes / después

| Sección del spec | Antes (IMPL-20260821-03) | Después (IMPL-20260821-04) |
|---|---|---|
| (a)..(g) viewport + max-height + bloque-1 visible + stepper/footer in viewport + aria-current inicial + prev disabled + altura mínima botones | PASS | PASS |
| (h) Click Siguiente → bloque-2 visible, bloque-1 oculto, aria-current en paso 2, prev/next habilitados, bodyScrollTop=0 | **FAIL** línea 373 (`afterNext.visible.bloque1).toBe(false)` → Received: true) | **PASS** |
| (i) Click Siguiente otra vez → bloque-3 visible, next deshabilitado | **FAIL** (no llegaba) | **PASS** |
| (j) Click Anterior → bloque-2 visible, bloque-3 oculto | **FAIL** (no llegaba) | **PASS** |

---

## 4. Estado de hallazgos

### F-5 (IMPL-20260821-03 §4) — cierre
- Estado: **CERRADO** por este IMPL. La nueva falla detectada al re-correr el gate mobile en IMPL-20260821-03 §4 — el fixture HTML inline del spec no emulaba el handler JS de navegación del stepper — queda resuelta con la adición del `<script>` inline en el fixture del propio spec. El test ahora pasa completo (1.4 s efectivo del test tras el webserver cold start) cubriendo las 11 secciones (a..k) y todas las assertions. **Causa raíz confirmada:** ausencia de listener `onClick` en los botones `[data-testid="entrevista-stepper-next"]`, `[data-testid="entrevista-stepper-prev"]` y `[data-testid="entrevista-stepper-step-N"]` del fixture HTML.

### F-4 (IMPL-20260821-03) — sigue cerrado
- Estado: **CERRADO** por IMPL-20260821-03 (sin cambios en este IMPL).

### Otros hallazgos de QA-20260821-01 — sin cambio de estado
- **F-2 [P3]** (regex `max-h-\[90dvh\]` en unit test): sin cambio.
- **F-3 [P3]** (`discovery/FINDINGS.md` e `discovery/INDEX.md` modificados sin scope): sin cambio. Decisión de ATLAS + Frank.

---

## 5. Estado por entorno

| Entorno | Estado | Razón |
|---|---|---|
| **Calidad (sandbox)** | **READY_FOR_VERIFYING** | F-5 cerrado y validado: el spec del stepper pasa completo (todas las secciones a..k) y la regresión mobile existente (`entrevista-inicial.mobile.spec.ts`, AC-10 IMPL-20260820-04) sigue verde. typecheck/lint/test/build PASS; unit cubre UX-1..UX-10 sin regresión. Gate MOBILE-1 (e2e mobile) en verde para ambos specs. |
| **Staging** | **NO_LISTO** | Decisión fuera de este IMPL (F-3 discovery pendiente de triage ATLAS+Frank; staging requiere OK explícito vigente de Frank). No aplica `supabase db push` (IMPL UX de testing, sin migraciones, sin secretos). |
| **Producción** | **NO_LISTO** | Consecuencia: staging no listo + sin OK explícito vigente de Frank en esta conversación. No aplica `supabase db push` para este IMPL UX de testing. |

---

## 6. Handoff a ATLAS

- **Acción para ATLAS:**
  - **F-5 (P3): CERRADO** por este IMPL. Diff contenido al `<script>` inline añadido al fixture HTML del spec (líneas 222-284) + comentario HTML explicativo; sin tocar producto, componentes, discovery ni contratos.
  - **F-4 (P2): sin cambio de estado** (cerrado por IMPL-20260821-03).
  - **F-2, F-3 (QA-20260821-01):** sin cambio de estado respecto al handoff previo.
- **Gate siguiente (post-fix SOFIA F-5):**
  1. INTEGRA verifica F-5 (puede correr `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — todos PASS — y `pnpm exec playwright test e2e/entrevista-inicial-stepper.mobile.spec.ts --project=mobile` y `pnpm exec playwright test e2e/entrevista-inicial.mobile.spec.ts --project=mobile` — ambos PASS — y confirmar que el diff del spec es estrictamente el `<script>` añadido al fixture + comentario inline).
  2. GEMINI audita el incremento (alcance único: spec de testing) y devuelve `QA-20260821-02` con `PASS`/`PASS_WITH_WARNINGS`.
  3. ATLAS pide OK a Frank para promover a staging (junto con F-3 de discovery resuelto por ATLAS/Frank).
  4. Producción: requiere OK explícito de Frank (no aplica `supabase db push` para este IMPL UX de testing; sin secretos; sin migraciones).
- **Requiere GEMINI:** no para F-5 (es fix contenido de un único spec e2e, sin contrato público, sin producto, sin dependencias nuevas, sólo JS inline en el fixture).
- **Requiere DEBY:** no (no hay bug reproducible runtime; el fallo era de fixture de testing, ya diagnosticado y resuelto por este IMPL).

---

## 7. Notas de reversión

Reversión segura del fix F-5 sin pérdida de datos:
1. `git checkout -- e2e/entrevista-inicial-stepper.mobile.spec.ts` (restaura la versión sin el `<script>` inline del stepper). Como el archivo está untracked en esta rama, el equivalente es `rm e2e/entrevista-inicial-stepper.mobile.spec.ts` y reaplicar la versión pre-IMPL-20260821-04 desde el origen correspondiente.
2. `rm specs/IMPL-20260821-04_report.md`.
3. No hay migraciones que revertir; no hay datos que migrar; no hay secretos que rotar.
4. Verificación: `pnpm typecheck && pnpm lint && pnpm test && pnpm build` → todos PASS con la suite baseline (290 tests). El gate Playwright vuelve a fallar en la sección (h) línea 373 (`Received: true` por F-5 reabierto), comportamiento documentado en IMPL-20260821-03 §4.

El fix F-5 **no introduce** comportamiento nuevo en código de producto; sólo añade un handler JS inline al fixture del spec para que pueda ejecutar el flujo de clicks del stepper. El componente real `components/alumnos/entrevista-inicial-form.tsx` ya tenía ese comportamiento; el fixture simplemente no lo emulaba.

---

## 8. Autoauditoría final

- [x] Único cambio autorizado por Frank: el archivo `e2e/entrevista-inicial-stepper.mobile.spec.ts`, sólo el `<script>` inline añadido al fixture HTML antes del `</body>` + comentario HTML `<!-- ... -->` no funcional.
- [x] No inventé comportamiento, contrato ni dependencia. El handler emulado replica literalmente lo que el componente real `components/alumnos/entrevista-inicial-form.tsx` ya hace (mismo conjunto de selectores, misma semántica de `hidden`/`aria-current`/`disabled`/`scrollTop`).
- [x] No edité `app/`, `components/`, `services/`, `tests/`, `supabase/`, `discovery/`, `SPEC_TEC_*`, `ADR-*` ni `PROYECTO.md`.
- [x] No inserté IDs en código (`IMPL-20260821-04` aparece sólo en este reporte y en el comentario HTML del spec que documenta el propósito del `<script>`, no como marca de agua funcional).
- [x] Revisé el diff contra el ticket: las únicas modificaciones son el `<script>` que emula la navegación + el comentario HTML explicativo.
- [x] Cada criterio (F-5.a..F-5.i) tiene prueba o evidencia listada en §2.
- [x] Declaré lo ejecutado (Playwright mobile stepper + mobile regresión, typecheck, lint, test, build) y lo no modificado (producto, componentes, discovery, tests unit, SPEC, ADR).
- [x] F-5 cerrado (verificado: spec del stepper pasa completo, secciones a..k verdes; regresión mobile existente sigue verde).
- [x] Estado: **`READY_FOR_VERIFYING`** (alcance del ticket cumplido); gate MOBILE-1 verde en sandbox para ambos specs mobile.

---

**Fin del reporte.** F-5 cerrado con cambio mínimo y reversible (~60 líneas de `<script>` inline + comentario HTML en el fixture del spec, sin tocar producto, discovery ni contratos). Validaciones locales: typecheck PASS, lint PASS (1 warning preexistente no relacionado), tests PASS (290/3 skipped), build PASS, Playwright mobile stepper PASS, regresión mobile existente PASS. Sin commit/push/deploy.
