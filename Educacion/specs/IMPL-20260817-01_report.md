# Reporte IMPL-20260817-01 — Kickoff MVP NEM (sesión 1: setup base + Path A)

**ID intervención:** IMPL-20260817-01
**ID tarea:** SOFIA-IMPL-MVP-2026-08-17 (delegación Frank → SOFIA)
**SPEC:** `Educacion/specs/SPEC_TEC_01..06.md` (6 specs production-ready, 5,981 líneas)
**Handoff INTEGRA:** `GO_FINAL_ABSOLUTO_2026-08-17.md` + `V3_DECISION_PATH_A_B.md` + `CHECKLIST_PRESOFIA_2026-08-17.md`
**Fecha:** 2026-08-17 14:30 UTC-6
**Autor:** SOFIA (constructora determinista)
**Sesión:** 1 de N (alcance MVP = 17 áreas funcionales; sesión 1 cubre setup base + 1 de 17 items)

---

## 1. RESUMEN EJECUTIVO

Kickoff del MVP. **2 entregables cerrados en esta sesión:**

1. **Path A SQL aplicado** (tarea específica de INTEGRA-V3): `cli.py build-sql` ahora genera SQL con `codigo TEXT PRIMARY KEY` + FKs por texto para las 8 tablas catálogo (alineado con `SPEC_TEC_02 §5`). Tablas misceláneas (`referencia_libro_conaliteg`, `auditoria_carga`) mantienen `id SERIAL` por diseño §5.1.9/§5.1.10. SQL regenerado y validado con `pglast` (44 statements OK).

2. **Monorepo Next.js 14 base inicializado**: TypeScript strict + Tailwind + shadcn/ui config + 3 clientes Supabase (browser/server/middleware) + Husky pre-commit + estructura de carpetas canónica según `SPEC_TEC_04 §3` (54 directorios con `.gitkeep`). Compila, typechea, lintea y build-ea sin errores.

**Progreso MVP:** 2/17 items del orden de implementación (~12%). Resto en sesiones siguientes.

---

## 2. TAREAS COMPLETADAS

### 2.1 Path A SQL (tarea específica de INTEGRA)

| # | Acción | Estado |
|---|--------|--------|
| 1 | Modificar `modelador.py`: PKs serial→text en 8 tablas catálogo | ✅ |
| 2 | Ajustar FKs: `INT REFERENCES tabla(id)` → `TEXT REFERENCES tabla(codigo)` | ✅ |
| 3 | Simplificar INSERTs: eliminar subqueries `(SELECT id FROM ... WHERE codigo=X)` | ✅ |
| 4 | Regenerar `outputs/migrations/2026-08-17_catalogo_fase2.sql` | ✅ |
| 5 | Validar sintaxis con `pglast` (44 statements OK) | ✅ |
| 6 | Verificar PKs por tabla (8 text + 2 serial) | ✅ |

**Tablas con PK text (Path A):** `catalogo_version`, `campo_formativo`, `eje_articulador`, `fase`, `pda`, `contenido`, `pda_por_campo_fase`, `pda_ejes`.
**Tablas con PK serial (mantienen §5.1.9/§5.1.10):** `referencia_libro_conaliteg` (id int), `auditoria_carga` (id serial legacy; §5.1.10 quiere uuid → issue L3 pendiente).

### 2.2 Setup monorepo Next.js 14

| # | Acción | Estado |
|---|--------|--------|
| 1 | `package.json` con 30+ deps (Next 14, React 18, Supabase ssr, dnd-kit, zustand, zod, react-hook-form, lucide, radix, jsonwebtoken, jose, puppeteer-core, playwright) | ✅ |
| 2 | `tsconfig.json` strict + `noUncheckedIndexedAccess` + `noImplicitOverride` + path alias `@/*` | ✅ |
| 3 | `next.config.mjs` con headers X-Frame-Options para `/v/[entrega_id]` | ✅ |
| 4 | `tailwind.config.ts` con paleta NEM (verde/amarillo/naranja/rojo) + CSS vars shadcn + dark mode class | ✅ |
| 5 | `postcss.config.mjs`, `.env.example` (28 vars), `.gitignore`, `.eslintrc.json`, `.prettierrc`, `.editorconfig`, `components.json` (shadcn) | ✅ |
| 6 | `playwright.config.ts` (Chromium + mobile) + `vitest.config.ts` (≥80% cobertura) | ✅ |
| 7 | `app/layout.tsx` (root, viewport, metadata, manifest) + `app/page.tsx` (landing pública) + `app/globals.css` (paleta + dark + dnd touch-action) | ✅ |
| 8 | `app/manifest.ts` (PWA: theme verde NEM, icons 192/512/maskable) | ✅ |
| 9 | `app/(auth)/{layout,login/page}.tsx` + `app/(app)/{layout,dashboard/page}.tsx` (stubs navegables) | ✅ |
| 10 | `middleware.ts` raíz + `lib/supabase/{client,server,middleware}.ts` (3 entrypoints, tipos CookieOptions) | ✅ |
| 11 | `types/database.ts` (stub con shape de las 12 tablas catálogo) + `types/domain.ts` (UserRole, EstadoPlaneacion, NivelSemaforo, Modalidad) | ✅ |
| 12 | `lib/utils.ts` (cn, formatDate, truncate) + `tests/helpers/vitest-setup.ts` | ✅ |
| 13 | Husky pre-commit ejecutable (lint-staged: prettier + eslint) | ✅ |
| 14 | 54 directorios vacíos con `.gitkeep` según SPEC_TEC_04 §3 | ✅ |

### 2.3 Validaciones ejecutadas

| Validación | Comando | Resultado |
|------------|---------|-----------|
| Install | `pnpm install --prefer-offline` | ✅ 0 errores (28.9s) |
| Typecheck | `pnpm typecheck` (tsc --noEmit) | ✅ 0 errores (strict + noUncheckedIndexedAccess) |
| Lint | `pnpm lint` (next lint) | ✅ 0 warnings, 0 errors |
| Build | `pnpm build` (Next.js 14) | ✅ 5 páginas estáticas + middleware (85kB) |
| SQL Path A | `pglast.parse_sql()` | ✅ 44 statements parseados |
| SQL PKs | regex check | ✅ 8 text + 2 serial (correcto) |

---

## 3. ARCHIVOS MODIFICADOS / CREADOS

### 3.1 Modificados (1 archivo)

- `Educacion/scripts/catalogar/modelador.py` (+70/-50 líneas netas) — Path A: PKs text + FKs text en 8 tablas catálogo; SQL_HEADER documenta delta pendiente; `referencia_libro_conaliteg` y `auditoria_carga` mantienen id SERIAL (alineado con §5.1.9/§5.1.10).

### 3.2 Regenerados (1 archivo, no se commitea sin OK)

- `Educacion/scripts/catalogar/outputs/migrations/2026-08-17_catalogo_fase2.sql` (Path A, 44 statements, 8 tablas text-PK + 2 serial)

### 3.3 Creados (27 entries en git status, ~45 archivos contando subcarpetas)

**Configs raíz (13):**
- `Educacion/package.json`
- `Educacion/tsconfig.json`
- `Educacion/next.config.mjs`
- `Educacion/tailwind.config.ts`
- `Educacion/postcss.config.mjs`
- `Educacion/.env.example` (28 vars documentadas)
- `Educacion/.gitignore`
- `Educacion/.eslintrc.json`
- `Educacion/.prettierrc`
- `Educacion/.editorconfig`
- `Educacion/components.json` (shadcn)
- `Educacion/playwright.config.ts`
- `Educacion/vitest.config.ts`

**Next.js (7):**
- `Educacion/app/layout.tsx`
- `Educacion/app/page.tsx`
- `Educacion/app/globals.css`
- `Educacion/app/manifest.ts`
- `Educacion/app/(auth)/layout.tsx` + `login/page.tsx`
- `Educacion/app/(app)/layout.tsx` + `dashboard/page.tsx`
- `Educacion/middleware.ts`

**lib + types (5):**
- `Educacion/lib/supabase/client.ts`
- `Educacion/lib/supabase/server.ts`
- `Educacion/lib/supabase/middleware.ts`
- `Educacion/lib/utils.ts`
- `Educacion/types/domain.ts` + `types/database.ts`

**Tests + Husky (2):**
- `Educacion/tests/helpers/vitest-setup.ts`
- `Educacion/.husky/pre-commit` (ejecutable)

**Estructura vacía (54 .gitkeep):** 21 módulos UI/dominio según SPEC_TEC_04 §3.

---

## 4. EVIDENCIA REPRODUCIBLE

```bash
# Path A
cd Educacion/scripts/catalogar && python3 cli.py build-sql --input outputs/catalogo_fase2_v2024_crudo.json --output-dir outputs --fecha 2026-08-17
# → outputs/migrations/2026-08-17_catalogo_fase2.sql (44 statements)

# Validar
python3 -c "import pglast; pglast.parse_sql(open('outputs/migrations/2026-08-17_catalogo_fase2.sql').read()); print('OK')"

# Monorepo
cd Educacion
pnpm install --prefer-offline    # 28.9s, 0 errores
pnpm typecheck                    # 0 errores (strict)
pnpm lint                         # 0 warnings/errors
TMPDIR=$(pwd)/.cache/tmp pnpm build  # 5 páginas + middleware OK
```

---

## 5. DECISIONES INTERNAS (no requieren aprobación Frank; reversibles)

| Decisión | Justificación |
|----------|---------------|
| `referencia_libro_conaliteg` y `auditoria_carga` mantienen `id SERIAL` | SPEC_TEC_02 §5.1.9/§5.1.10 las define así. Auditoria §5.1.10 quiere uuid → queda como issue L3 (no Path A). |
| Nombre de columna FK con sufijo `_id` pero tipo TEXT (no renombrar a `_codigo`) | Mínimo cambio para preservar compatibilidad con el JSON del catálogo y el modelo pydantic. Renombrar a `_codigo` rompería inserts. |
| `tailwind.config.ts` (TypeScript) en vez de `.js` | SPEC_TEC_04 §3 lo especifica; requiere `@vitejs/plugin-react` para vitest. |
| `recurso_skill` tabla NO creada en migraciones iniciales | SPEC_TEC_04 §5.4: para MVP la matching bloque-recurso usa solo campo `uso` (más simple). Tabla queda para Fase 2. |
| Stub `types/database.ts` | Generador real requiere `supabase start` + `supabase gen types`; lo creo placeholder para que el typecheck no rompa hasta primera ejecución. |
| `middleware.ts` aplica redirect a `/login` desde rutas `(app)/*` | SPEC_TEC_04 §7.3; cubre R-01 (proteger antes de capturar alumnos). |
| `PDF_GENERATOR=playwright` por default | SPEC_TEC_05 §4.2.3; PEND-04-05 (migrar a externo si >10s) queda para Fase 2. |
| Husky solo pre-commit (no pre-push) | PEND-04-06 explícito: pre-push "no en MVP (más lento)". |

---

## 6. RIESGOS / DESVIACIONES

### 6.1 Delta pendiente en modelo de datos (no bloqueante MVP, documentado)

`SPEC_TEC_02 §5` agrega columnas que el JSON actual no tiene. El seed Path A funciona, pero la aplicación que consulte estas tablas las encontrará faltantes hasta que se complete la migración de alineación. **Issues L3-NEW-01..03** documentados en `CHECKLIST_PRESOFIA_2026-08-17.md §2`.

| Tabla | Columna §5 | Estado en JSON | Acción |
|-------|------------|----------------|--------|
| `pda` | `grado TEXT NOT NULL` | ❌ falta | Migración alineada |
| `pda` | `contenido_codigo TEXT NOT NULL` | ❌ falta | Migración alineada |
| `contenido` | `requiere_revision_humana BOOLEAN` | ✅ existe (pydantic) | OK |
| `contenido` | `razon_revision TEXT` | ✅ existe (pydantic) | OK |
| `referencia_libro_conaliteg` | `no_verificada`, `tipo`, `formato` | ❌ falta | Migración alineada |
| `auditoria_carga` | `id uuid` (no serial) | ❌ usa serial | Migración alineada |
| `auditoria_carga` | sin `pda_id` | ❌ existe pda_id | Migración alineada |

**Severidad:** L3 cosmética, no bloqueante MVP. **Acción para INTEGRA:** decidir si la migración de alineación se hace antes de Fase 1 con docentes reales o se difiere a Fase 2.

### 6.2 Cambios sin commitear del catalogador (no míos)

Pre-existen 20 staged renames (`catalogar/` → `scripts/catalogar/`) + 10 unstaged modifications del refactor del catalogador. **No toqué esos cambios** (son trabajo previo de INTEGRA). Mis cambios a `modelador.py` se **suman** al diff unstaged de ese archivo.

### 6.3 15/17 items del orden MVP no iniciados

Esta sesión cubrió setup base + 1 tarea (Path A). Los 15 items restantes (onboarding, catálogo frontend, M1 bloques, wizard planeación, recursos aula, rúbrica, PDF generation, PDF viewer, notificación director, aviso privacidad, multi-grupo, tests, PWA, migraciones Supabase DDL) requieren sesiones dedicadas.

---

## 7. PENDIENTES INTEGRA / GEMINI

1. **Solicitar revisión final a GEMINI** (subagent_type='gemini') como segunda mano de validación antes de cerrar como DONE. Este cambio no es trivial: introduce ~45 archivos nuevos, modifica 1 archivo crítico de generación SQL, redefine el modelo de datos del catálogo. Audita:
   - Coherencia de estructura con SPEC_TEC_04 §3
   - Consistencia de path alias `@/*` y barrel exports
   - Validación de que las 3 env vars críticas (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, JWT_SECRET) están en `.env.example` y referenciadas correctamente
   - Que el SQL Path A generado es ejecutable (ejecutar contra PostgreSQL real y verificar carga de 200+ registros)
2. **Frank aprueba commit** — NO commiteo sin OK explícito (regla dura + modo nocturno).
3. **Decisión sobre delta §5 vs JSON** — issue L3 (R2) tiene 3 sub-items cosméticos. Migración de alineación o diferir a Fase 2.

---

## 8. PRÓXIMOS PASOS (orden propuesto, sesión 2+)

| Sesión | Items del MVP | Salida esperada |
|--------|---------------|-----------------|
| **2** | #2 Supabase config + #3 Migraciones SQL (DDL completo §5) | `supabase/migrations/0001..0013.sql` ejecutables |
| **2** | #4 Auth Supabase (magic link) + #15 Multi-grupo UI (GrupoSelector dropdown) | Login funcional, RLS activo |
| **3** | #14 Aviso privacidad modal + #13 Onboarding 5 pantallas | Flujo D-FIN-4 + D-FIN-15 |
| **3** | #5 Catálogo NEM frontend (24 PDA + 4 contenidos + 7 ejes) | `services/catalogo/*` + componentes |
| **4** | #6 Catálogo M1 bloques (~50 MVP) + #7 Wizard planeación 5 pasos | D-FIN-1 + D-FIN-6 |
| **4** | #8 Recursos aula + F-IA1 NLP on-write | E21 §5 |
| **5** | #9 Rúbrica 4 niveles semáforo + @dnd-kit | D-FIN-2 + D-FIN-3 |
| **5** | #10 PDF generation Playwright + #11 PDF viewer CONALITEG | D-FIN-5 + D-FIN-10 |
| **5** | #12 Notificación director wa.me | D-FIN-19 |
| **6** | #16 Tests (Vitest + RTL + Playwright, T-E2E-07 RLS BLOQUEANTE) | SPEC_TEC_06 |
| **6** | #17 PWA (service worker + manifest + offline) | SPEC_TEC_04 §10 |

**Estimación:** 5-7 sesiones más (depende de densidad de Frank y reescritura de specs si hay iteraciones).

---

## 9. NOTAS DE ROLLBACK (recomendadas, no ejecución)

Si Frank decide revertir el setup del monorepo:

```bash
# Rollback selectivo: borrar solo archivos del monorepo (preservar catalogar y trabajo previo)
cd /home/frank/repos/educacion
git clean -fd Educacion/{app,lib,types,hooks,stores,services,supabase,public,tests,docs,context,components,middleware.ts,package.json,tsconfig.json,next.config.mjs,tailwind.config.ts,postcss.config.mjs,.env.example,.gitignore,.eslintrc.json,.prettierrc,.editorconfig,components.json,playwright.config.ts,vitest.config.ts,.husky}
# Rollback Path A: git checkout HEAD -- Educacion/scripts/catalogar/modelador.py
# Eliminar output regenerado: rm Educacion/scripts/catalogar/outputs/migrations/2026-08-17_catalogo_fase2.sql
```

**NO TOCAR:** staged renames del catalogador, unstaged modifications del catalogador, archivos en `Educacion/specs/`, `Educacion/fuentes/`, `Educacion/scripts/catalogar/` excepto `modelador.py`.

---

**Estado:** ✅ IMPL-20260817-01 cerrado a nivel setup base. **NO** declarado `MVP DONE` (faltan 15/17 items). Esperando OK de Frank para commit + decisión sobre delta L3 + continuación sesión 2.
