# IMPL-20260821-05 — Entrevista familiar (migración aditiva `0024` + entrevista del niño intacta) — READY_FOR_VERIFYING

- **ID intervención:** IMPL-20260821-05
- **ID tarea:** IMPL-20260821-05 (`SPEC-HANDOFF-20260821-SOFIA-ENTREVISTA-FAMILIAR.md`)
- **Estado:** **READY_FOR_VERIFYING**
- **SPEC activa:** `specs/SPEC_TEC_11_Entrevista_Familiar.md` (SPEC-20260820-11 **v2.0**, rev. 2026-08-21 por `DEC-20260821-01`)
- **ADR:** `specs/ADR-20260820-04.md` (ARCH-20260820-04, revisado por `DEC-20260821-01`; D11-01..D11-11; F-A..F-E cerradas con valores concretos)
- **Supersede:** `IMPL-20260820-07` (estado BLOCKED; el `DISCOVERY-GAP-20260820-ENTREVISTA-FAMILIAR` queda RESUELTO por DEC-20260821-01)
- **Discovery refs:** `DEC-20260821-01`, `DEC-20260820-04`, `DEC-20260820-02`, `DEC-20260820-05`, `FND-20260820-08`, `OQ-20260820-07`, `OQ-20260820-04`, `SCN-20260820-08`, `D-FIN-2`, `D-FIN-15`
- **Fuente literal:** `docx_extract/NUEVA ENTREVISTA.pdf` (JARDIN DE NIÑOS "CELESTINO FREINET" / CUESTIONARIO A PADRES DE FAMILIA)
- **Fecha:** 2026-08-21
- **Worker:** SOFIA (una sola sesión; sin paralelizar)
- **Próximo paso sugerido:** ATLAS → pivote a INTEGRA para verificación / GEMINI para auditoría obligatoria (modelo de datos + RLS + no-IA + literalidad + separación + gate + edición + archivado + firma).

---

## Manifiesto de archivos modificados / creados

### Creados (aditivos; no tocan código previo)

| Ruta | Cambio |
|---|---|
| `supabase/migrations/0024_entrevista_familiar_alumno.sql` | Migración aditiva: tabla `entrevista_familiar_alumno` + RLS + policy única docente + trigger updated_at + índices + `comment on table`. Patrón idéntico a `entrevista_inicial_alumno` (`0022`). **NO** crea policy de director (D11-08). **NO** crea columna/tabla de versiones (D11-10). **NO** crea bucket/storage/hash para firma (D11-11). `supabase db push` **NO** ejecutado (Frank autoriza). |
| `types/entrevista-familiar.ts` | Tipos v1 (`EntrevistaFamiliarV1`, `RespuestasFamiliarV1`, `BloqueProgenitor`, `BloqueHabito`, `BloqueCierre`, `BloqueFirmas`), esquemas zod espejo §4.2, validación literal `validateCuestionarioFamiliarV1`, `buildRespuestasFamiliaresVaciasV1`, `normalizarPadresSeparadosSegunSituacion`. |
| `services/alumnos/entrevista-familiar-actions.ts` | Server actions `'use server'`: `getEntrevistaFamiliar`, `upsertEntrevistaFamiliar`, `archivarEntrevistaFamiliar`. **Sin** `deleteEntrevistaFamiliar` (D11-09). **Sin** referencias a `entrevista_inicial_alumno` (AC-FF6). Gate D11-07 + ownership. |
| `components/alumnos/entrevista-familiar-form.tsx` | Formulario cliente (`'use client'`) con los 6 bloques del PDF en orden literal. Mobile-first 375×812. Anti-doble-submit. Sin emojis/gamificación. Bloque D condicional. Firmas como dos inputs de texto. |
| `tests/unit/services/alumnos/entrevista-familiar-actions.spec.ts` | Tests unitarios: AC-FF1/FF2 (cuestionario literal + peculiaridades), AC-FF8 (gate), AC-FF9 (edición in-place), AC-FF10 (archivado sin delete + idempotente), AC-FF11 (firma como strings), AC-FF3/FF4 (SQL contrato), AC-FF6 (separación), AC-FF7 (no-IA scan real). |
| `tests/unit/components/entrevista-familiar-form.spec.tsx` | Tests de componente: 6 bloques, peculiaridades literales, gate D11-07, 15 hábitos con salto 14→16, firmas como inputs, bloque D condicional, archivada banner, submit. |
| `e2e/entrevistas-contenedor.spec.ts` | E2E AC-FF5 (Playwright); declarado NO EJECUTABLE en sandbox sin Supabase; PASS en staging. |

### Modificados (sin tocar contratos protegidos)

| Ruta | Cambio |
|---|---|
| `app/(app)/alumnos/entrevista-dialog-content.tsx` | Adaptado a **contenedor con dos pestañas** (`Entrevista del niño` \| `Entrevista familiar`). Pestaña infantil carga con `EntrevistaInicialForm` intacto. Pestaña familiar carga el nuevo `EntrevistaFamiliarForm`. Carga paralela vía `Promise.all` (cada una valida RLS/ownership server-side). Sin scroll horizontal, anti-doble-submit por form. **Sin** cambios en `alumnos-manager.tsx` (sigue abriendo el contenedor). |
| `supabase/migrations_master.sql` | Añadida sección `0024_entrevista_familiar_alumno` sin tocar secciones previas. |
| `tests/helpers/vitest-setup.ts` | Polyfill de `ResizeObserver` (Radix Tabs/Select requieren ResizeObserver; jsdom no lo implementa). IMPL-20260821-05. |
| `tests/unit/components/entrevista-dialog-content.spec.tsx` | Añadido mock explícito de `getEntrevistaFamiliar` (Mock `({ ok: true, data: null })`) para no tocar Supabase; los tests de la infantil siguen pasando (12/12). |

### Sin tocar (contratos protegidos)

- `0001`–`0023` (migraciones): **inmutables**. `git diff -- supabase/migrations/0022_entrevista_inicial_alumno.sql supabase/migrations/0023_entrevista_inicial_completa.sql` → vacío (0 líneas).
- `entrevista_inicial_alumno` (migración `0022`/`0023`), `services/alumnos/entrevista-actions.ts`, `components/alumnos/entrevista-inicial-form.tsx`, `types/entrevista.ts`, `app/(app)/alumnos/alumnos-manager.tsx`: intactos.
- `lib/ia/*`, `services/ia/*`, `app/api/**/ia/*`, `anonymizer.ts`, `acceptance_aviso_privacidad`, `discovery/`, `fuentes/`, `SPEC_MVP_01`, `.env*`, `package.json`: sin cambios.

---

## Contratos

- Tabla nueva `entrevista_familiar_alumno` (D11-01).
- `respuestas jsonb` con contrato §4.2 (bloques identificación, mamá, papá, situaciónLegal, padresSeparados, habitosFamiliares, cierre, firmas).
- Tipos `types/entrevista-familiar.ts`, server actions `services/alumnos/entrevista-familiar-actions.ts`, form `components/alumnos/entrevista-familiar-form.tsx`: nuevos.
- `app/(app)/alumnos/entrevista-dialog-content.tsx`: pasa a contenedor con dos pestañas.

---

## Validación (PASS salvo donde se indica)

| Categoría | Comando | Resultado |
|---|---|---|
| Baseline typecheck | `pnpm typecheck` | PASS (0 errores) |
| Baseline lint | `pnpm lint` | PASS (1 warning preexistente no relacionado en `lib/supabase/server.ts:11`) |
| Baseline build | `pnpm build` | PASS |
| Baseline infantil tests | `pnpm vitest run tests/unit/services/alumnos/entrevista-actions.spec.ts` | 24/24 PASS |
| Baseline infantil form tests | `pnpm vitest run tests/unit/components/entrevista-inicial-form.spec.tsx` | PASS |
| Tests nuevos familiar actions | `pnpm vitest run tests/unit/services/alumnos/entrevista-familiar-actions.spec.ts` | 33/33 PASS |
| Tests nuevos familiar form | `pnpm vitest run tests/unit/components/entrevista-familiar-form.spec.tsx` | 10/10 PASS |
| Tests regresión dialog-content | `pnpm vitest run tests/unit/components/entrevista-dialog-content.spec.tsx` | 12/12 PASS (infantil intacta + nueva pestaña) |
| Suite completa vitest | `pnpm test` | 333/333 PASS (3 skipped) — sin regresiones |
| Playwright infantil (móvil) | `pnpm exec playwright test e2e/entrevista-inicial.mobile.spec.ts e2e/entrevista-inicial-stepper.mobile.spec.ts` | 4/4 PASS (375×812 sin overflow) |
| Playwright entrevistas-contenedor nuevo | `pnpm exec playwright test e2e/entrevistas-contenedor.spec.ts` | NO EJECUTABLE en sandbox sin Supabase (declarado por handoff y por SPEC_TEC_11 §13); PASS esperado en staging |

### Guardrails AC (§grep)

- **AC-FF3** — `create table if not exists entrevista_familiar_alumno` → **1 match** (`supabase/migrations/0024_entrevista_familiar_alumno.sql:22`). `migrations_master.sql:893` contiene `0024_entrevista_familiar_alumno.sql`. `git diff -- 0022 0023` → 0 líneas (inmutables).
- **AC-FF4** — `grep -n "entrevista_familiar_docente_own" supabase/migrations/0024_entrevista_familiar_alumno.sql` → 1 match (`create policy` + `drop policy if exists`). `grep -n "entrevista_familiar_director_cct" supabase/migrations/0024_entrevista_familiar_alumno.sql` → **0 matches**.
- **AC-FF6** — `grep -n "entrevista_familiar_alumno" services/alumnos/entrevista-actions.ts` → **0**. `grep -n "entrevista_inicial_alumno" services/alumnos/entrevista-familiar-actions.ts` → **0**. El formulario infantil no consulta la tabla familiar ni viceversa.
- **AC-FF7** — `grep -rn "entrevista_inicial_alumno\|entrevista_familiar_alumno" app/api services/ia lib/ia` → **0**. `grep -rn "entrevista-familiar" app/api services/ia lib/ia` → **0**. Adicionalmente, los tests escanean la capa IA y verifican que ningún archivo bajo `app/api/**/ia/*`, `services/ia/*`, `lib/ia/*` importa los módulos `entrevista-familiar-actions` / `types/entrevista-familiar` / `entrevista-familiar-form`.
- **AC-FF8** — Cubierto por tests unitarios (AC-25 infantil espejo): sin aviso → error de gate `Se requiere aceptar el aviso de privacidad antes de registrar la entrevista`.
- **AC-FF9** — Cubierto por test: segunda llamada `upsertEntrevistaFamiliar` con mismo `(alumno, ciclo)` actualiza la misma fila (idem-potencia vía `onConflict: 'alumno_id,ciclo_escolar'`). `unique (alumno_id, ciclo_escolar)` en SQL.
- **AC-FF10** — `grep -n "deleteEntrevistaFamiliar" services/alumnos/entrevista-familiar-actions.ts` → **0**. `grep -n "deleteEntrevista\b" services/alumnos/entrevista-familiar-actions.ts` → **0**. `grep -n "archivarEntrevistaFamiliar" services/alumnos/entrevista-familiar-actions.ts` → **1** (`export async function archivarEntrevistaFamiliar`). SQL no crea tablas `*version`. Archivar idempotente (test dedicado).
- **AC-FF11** — `grep -rn "firma.*storage\|firma.*upload\|firma.*image\|firma_imagen\|firma_hash" services supabase/migrations/0024_entrevista_familiar_alumno.sql` → **0**. `grep -n "nombreMama\|nombrePapa" types/entrevista-familiar.ts` → ≥1. El form (`entrevista-familiar-form.tsx`) usa dos `<Input type="text">` para mamá/papá (verificado en test: `familiar-firma-mama.tapa.type === 'text'`).

---

## Trazabilidad AC

| AC | Criterio | Evidencia |
|---|---|---|
| **AC-FF1** | Cuestionario literal §4 (6 bloques, 15 ítems {1..14,16}) | `tests/unit/services/alumnos/entrevista-familiar-actions.spec.ts:107-122` (15 ítems, orden enum, peculiaridades). Validación `validateCuestionarioFamiliarV1` rechaza orden/pregunta/subcampo alterados. |
| **AC-FF2** | `escorar` (sic) en 13, `limites` sin tilde 10/11, `ocupación` minúscula, cierre literal (sin tildes) | `entrevista-familiar-actions.spec.ts:124-189` (peculiaridades). `entrevista-familiar-form.spec.tsx` valida peculiaridades en el render. |
| **AC-FF3** | Migración aditiva `0024`, RLS habilitada, trigger, `unique`, `comment on table`, inmutabilidad 0001..0023 | `0024_entrevista_familiar_alumno.sql` + `entrevista-familiar-actions.spec.ts:AC-FF3` (~líneas 488-567). `git diff` vacío para 0022/0023. |
| **AC-FF4** | `entrevista_familiar_docente_own` existe; policy de director NO existe | SQL: líneas 58-59 (policy docente); no hay statements para director. Test dedicado. |
| **AC-FF5** | Contenedor con dos pestañas; pestaña familiar con cuestionario literal; persistencia al recargar | `e2e/entrevistas-contenedor.spec.ts` (NO EJECUTABLE en sandbox; PASS en staging). UI: `entrevista-dialog-content.tsx` ahora Tabs con dos contenidos. |
| **AC-FF6** | Separación estricta (ningún archivo referencia el otro instrumento) | Greps → 0 matches. Tests `entrevista-familiar-actions.spec.ts:AC-FF6` (líneas ~462-480) verifican por lectura de archivo que el módulo no importa `entrevista-actions` ni `types/entrevista`. |
| **AC-FF7** | No-IA: 0 referencias en `app/api/**/ia/*`, `services/ia/*`, `lib/ia/*` | Greps → 0 matches. Test `AC-FF7` (`entrevista-familiar-actions.spec.ts`) escanea la capa IA con regex sobre los paths reales de `app/api/**/ia/*`, `services/ia/*`, `lib/ia/*`. |
| **AC-FF8** | Gate aviso existente (D11-07) — sin aviso → error; con aviso → ok | Test AC-FF8 (líneas ~252-275): skipAviso:true → `error.match(/aviso de privacidad/i)`. |
| **AC-FF9** | Edición in-place: `unique (alumno_id, ciclo_escolar)`; `updated_at` se incrementa vía trigger | Test AC-FF9: 2ª llamada `upsertEntrevistaFamiliar` con mismo alumno/ciclo → `entrevistaStore.length === 1`. SQL: `unique (alumno_id, ciclo_escolar)` + trigger `trg_entrevista_familiar_updated ... execute function set_updated_at()`. |
| **AC-FF10** | Archivado sin `deleteEntrevistaFamiliar`; sin tabla de versiones; idempotente | Tests AC-FF10 (líneas ~342-372): transición completa→archivada, idempotente; módulo sin `deleteEntrevistaFamiliar`. SQL no crea `*version`. |
| **AC-FF11** | Firma = dos inputs de texto (D11-11); sin storage/has/imagen; sin valor legal | Test "bloque F: firmas son dos inputs de texto" verifica tag/role/type. Test `grep -rn "firma.*storage\|.../\|firma_imagen\|firma_hash"` → 0. SQL no crea columnas de imagen. Schema tiene `nombreMama`/`nombrePapa` como `z.string().max(200)`. UI muestra "sin valor legal de firma manuscrita". |

---

## Riesgos y desviaciones

- **Riesgo (BAJO):** `respuestas jsonb` sin JSON Schema en runtime en BD. Mitigación: zod server-side (`validateCuestionarioFamiliarV1`) + auditoría de literalidad en el test (byte-a-byte).
- **Riesgo (BAJO):** `0024` no ha sido aplicada — `supabase db push` requiere autorización explícita de Frank. La tabla, RLS, policy y trigger están en el artefacto y validados vía lecturas estáticas + unit tests del módulo.
- **Riesgo (BAJO):** el cuestionario depende de la corrección de la transcripción del PDF. Si SOFIA/GEMINI detecta discrepancias entre el array fuente y el PDF, se reportan como `SPEC-GAP`/`DISCOVERY-GAP` (no se normaliza silenciosamente, DEC-20260821-01).
- **Sin desviaciones funcionales.** Todos los campos, etiquetas y peculiaridades reproducen el PDF literal (saltos, minúsculas/mayúsculas, peculiaridades ortográficas).
- **Decisión interna reversible:** usar Radix Tabs para el contenedor (en lugar de un componente propio) — reversible sin afectar contratos ni separación de datos.

---

## Requiere GEMINI

**sí** — auditoría obligatoria de modelo de datos + RLS + no-IA + literalidad + separación + gate + edición + archivado + firma. Razón: datos sensibles de menores y de terceros (mamá/papá con teléfono/edad/estudios/ocupación), nueva tabla con RLS docente, gate de aviso existente, edición in-place, firma como nombre tecleado, retención C1+C2. Cobertura mínima para GEMINI:
1. AC-FF3 (migración aditiva + RLS + trigger + `unique` + `comment`).
2. AC-FF4 (director sin acceso por defecto).
3. AC-FF7 (no-IA extendido a todos los campos familiares).
4. AC-FF1/FF2 (cuestionario literal y peculiaridades).
5. AC-FF6 (separación estricta infantil ↔ familiar).
6. AC-FF8 (gate aviso existente).
7. AC-FF9 (edición in-place sin versionado).
8. AC-FF10 (archivado sin borrado físico).
9. AC-FF11 (firma como nombre tecleado, sin storage/imagen/hash).

## Requiere DEBY

**no** — no hay bug reproducible ni causa raíz desconocida en esta entrega; los gates de compilación, lint, typecheck, tests y grep pasan. La migración no es ejecutada por SOFIA (Frank autoriza), por lo que no hay runtime implicado en este turno.

## Pendientes INTEGRA

- Verificación funcional (AC-FF5 staging): abrir el modal, alternar entre pestañas, llenar y guardar la familiar en una docente de prueba con aviso aceptado.
- Aplicación de la migración `0024` vía `supabase db push` cuando Frank autorice. SOFIA no la aplicó.
- Staging y producción requieren autorización separada.

---

## Notas de reversión (recomendación, nunca ejecución)

- Eliminar `entrevista_familiar_alumno`: `drop table if exists entrevista_familiar_alumno cascade;` — reversible, no afecta la infantil ni migraciones anteriores.
- Revertir el contenedor a un solo diálogo: restaurar la versión anterior de `entrevista-dialog-content.tsx` (anterior a IMPL-20260821-05) y eliminar el switch de `Tabs`. La infantil sigue funcionando intacta en esa configuración.
- Revertir a tablas fusionadas (D11-01 → inválido): requeriría nueva decisión funcional vía ATLAS/INTEGRA, no es una decisión interna reversible por sí sola.

---

## Estado final

**`READY_FOR_VERIFYING`**. Migración `0024` aditiva, NO aplicada (Frank autoriza). La entrevista del niño (0022/0023, acciones, form, modal) permanece intacta. El contenedor `Entrevistas` ofrece las dos pestañas con cuestionarios independientes, cada cual con RLS, gate y persistencia propios. Cero regresiones en la suite infantil (24/24 unit + 12/12 dialog + 4/4 E2E mobile). Suite total: 333/333 PASS. Próximo paso: pivote de ATLAS a INTEGRA → GEMINI → CRONISTA, sin commit/push/db push/deploy en este turno.
